# Design Map audit log

This prototype is intentionally a fixture-backed HLD surface. Each implementation commit was verified before moving to the next slice.

## Commits

| Commit | Change | Evidence |
| --- | --- | --- |
| `4e19e5e` | Link graph claims to revision-pinned source | Hosted architecture anchors, guided-flow steps, and exported trust evidence now expose direct source links when `meta.source_url_template` is supplied. Links preserve the bundle revision and exact file/line, while remaining absent when the exporter has no source template. TypeScript, projection smoke, and diff checks passed. |
| `4bc58bc` | Test source-template projection | Projection smoke now asserts that a revision-pinned source URL template survives bundle normalization, keeping the source-link contract executable. Projection smoke and diff checks passed. |
| `20dbdeb` | Keep source-link rendering lightweight | Hosted flow source links now use the already-loaded bundle metadata directly instead of rebuilding the full projection during render. TypeScript and diff checks passed. |
| `932e84d` | Surface the repository snapshot thesis | The graph-backed adapter now preserves `meta.description`, and the Architecture map presents that repository-authored description above the topology after validation. Projection smoke verifies metadata retention; TypeScript, production build, and diff checks passed. |
| `84c22e7` | Enforce comprehension-path minimums at intake | Design Map now rejects `code-understanding` bundles without a real entrypoint or at least one three-hop source-backed request path, preventing an upstream regression from becoming a blank comprehension surface. Projection smoke covers both rejection cases; TypeScript, production build, and diff checks passed. |
| `2f7cba3` | Expose hosted trust evidence safely | Graph-backed Trust now validates and renders exported evidence items with source-linked Lachesis handoffs, search, confidence/limitation context, and explicit non-claim language. Empty security evidence is explained rather than replaced with illustrative taxonomy. TypeScript, projection smoke, production build, full route smoke, and diff checks passed. |
| `a2c5a1d` | Prefer useful architecture concepts over file modules | The HLD adapter now accepts validated concepts and core-node references, uses a multi-concept semantic region model including authored overlap, retains concept descriptions, and falls back to modules for one catch-all concept. Projection smoke covered concept selection, relationship roll-up, and coarse fallback; the local Explorer demo bundle now exposes its three authored concepts and preserves cross-concept relationships deterministically. TypeScript, production build, and diff checks passed. |
| `17ed6d8` | Make graph-backed flow chapters deep-linkable | `/flows/[flow]?bundle=…` now accepts an exported request-path ID instead of limiting dynamic chapters to the illustrative Suricata route, and step history remains on the dedicated path. TypeScript, production build, full route smoke including `request.wsgi.app`, and diff checks passed. |
| `6eec9bc` | Read hosted request paths as architectural flows | Extended the local schema guard and projection boundary for code-understanding entrypoints and request paths, then replaced the hosted Flow dead end with a step-addressable, source-linked reading surface. The local Explorer demo bundle validated with one entrypoint, one request path, and eight nodes; projection smoke, TypeScript, production build, route smoke, and diff checks passed. The in-app browser was unavailable, so hydrated screenshot evidence remains pending. |
| `8f3e8ab` | Load a shared bundle directly on Start here | A maintainer-facing `/?bundle=…` link now validates and renders its bounded map on the distribution entry instead of asking the reader to open a second loader page. TypeScript, production build, full route smoke, and diff checks passed; fixture claims remain hidden while the bundle is unresolved. |
| `b928778` | Expose representative anchors for flat module projections | A focused graph-backed region now shows up to twelve source-backed module nodes when no child communities exist, with a deterministic remainder instead of an empty focus state. Projection smoke covered the representative-node fallback and the existing 0/1/8/30/500 scale matrix; TypeScript and diff checks passed. |
| `audit-2026-09-05` | Re-audit superseded implementation patterns | Source sweep found no obsolete `DocTabs`, generic `mode` graph composition, dashboard copy, force-directed implementation, or broad `transition: all` in the shipped UI. `/map` and `/flow` are intentional compatibility redirects; rejection-pattern terms in the design-system document are policy text only. |
| `audit-2026-09-05` | Keep hosted mobile region focus in Architecture | Mobile selection of a graph-backed region now stays in `/architecture` with the live `region` and `level=1` query context instead of entering the fixture-only region chapter. Illustrative regions retain chapter navigation. Hosted-default replay at 375px confirmed the focused projection, no unavailable fallback, and zero horizontal overflow. |
| `audit-2026-09-05` | Align source-optional bundle contract across the handoff | Explorer schema, verifier, and runtime validator now agree with Design Map that graph nodes may omit source snippets/windows; source-window structure remains validated, and a source-less regression bundle passes verification. Explorer typecheck, bundle verification, production build, and diff check passed. |
| `audit-2026-09-05` | Accept synthetic graph nodes with no path | The v2 schema permits an empty file path for synthetic or unmapped nodes. Design Map and Explorer now accept that case while retaining IDs, labels, kinds, and non-negative locations as required identity fields; projection and verifier regressions cover it. Both focused checks and diff checks passed. |
| `audit-2026-09-05` | Keep hydrated coverage provenance synchronized | Hosted-default Architecture hydration updated the snapshot badge but left the adjacent page-intro coverage label on the illustrative fixture scope. The label now subscribes to the same live snapshot event and reports `Coverage: repository` for `b_demo1234`; detector, check/build, hosted browser replay, zero-overflow, and diff checks passed. |
| `audit-2026-09-05` | Keep the compact repository bar compact | The live coverage fix is scoped to page-intro provenance; the repository bar no longer gains a duplicate long `Coverage:` label after hydration. Hosted-default mobile replay reports one page-intro coverage label, none in the bar, and zero overflow; detector, build, and diff checks passed. |
| `audit-2026-09-05` | Put the recommended action in the first viewport | A 375px viewport audit found the original Start CTA below the architecture map (around 1714px), weakening the required newcomer journey. The single primary `Follow a packet` action now precedes the map; Playwright places it at 634–682px, with the map beginning at 772px, zero overflow, and screenshot review passing. |
| `audit-2026-09-05` | Recheck Start at narrow mobile widths | Playwright measured Start at 320, 360, 375, and 414px. Every width kept zero document overflow, a 44px primary action, and the recommended CTA in the first viewport; the 320px action row stacks intentionally while preserving readable controls. |
| `audit-2026-09-05` | Recheck desktop Start hierarchy after CTA reorder | At 1440×1000, the repository thesis remains the first heading, the action row is a single editorial band, and the bounded map begins within the initial document view without overflow. The map remains the only spatial surface; screenshot inspection found no dashboard/card-grid drift. |
| `audit-2026-09-05` | Make top-level roll-ups explicit | The bounded projection now labels its macro remainder `Other N regions`, matching the brief’s stable “Other …” convention used for child roll-ups; the label retains the aggregated indexed-node footprint. Projection smoke covers 0–500 modules, the nine-region bound, build, and diff checks passed. |
| `audit-2026-09-05` | Re-run default hosted integration gates | Read-only replay against `https://lachesis.unboundcompute.com` passed hosted transport/recovery checks and the Explorer handoff smoke (`b_demo1234`: valid 2.0 bundle, 1 projected region, 7 relationships). No AWS resources were accessed; the artifact remains insufficient to prove multi-region production semantics. |
| `audit-2026-09-05` | Remove illustrative network claims from graph projections | Hosted `b_demo1234` revealed Architecture prose and canvas labels still said “network input,” “detection,” “wire input,” and “runtime spine” after hydration, despite representing an arbitrary graph. Graph-backed pages now use repository-neutral projection language and hide illustrative-only geometry; the Suricata fixture retains its authored vocabulary. Hosted production replay reports no external/axis labels, zero overflow, and the neutral lede. |
| `audit-2026-09-05` | Keep the Architecture continuation repository-neutral | The hosted graph-backed Architecture page still offered “Follow the packet flow,” which implied a Suricata-specific journey for arbitrary repositories. It now hands off directly to Lachesis with the selected snapshot context; the illustrative fixture retains its packet-flow continuation. Route smoke, typecheck, hosted build, browser replay, and diff checks passed. |
| `audit-2026-09-05` | Keep pending flow headings repository-neutral | Bundle-requested Flows pages exposed packet-specific headings before the graph was validated, even though their fixture steps were correctly hidden. Pending index and dedicated-flow routes now use neutral snapshot headings; unbundled Suricata routes retain the canonical packet narrative. Route smoke, detector, typecheck, and diff checks passed. |
| `audit-2026-09-05` | Keep hosted flow metadata share-safe | Pending graph-backed Flows routes were still emitting packet-specific Open Graph titles/descriptions. Metadata now uses repository-neutral “architectural flow snapshot” wording whenever `bundle` is present, while unbundled fixture previews keep the canonical packet description; route smoke covers both titles. |
| `audit-2026-09-05` | Retain nodes outside incomplete module catalogs | The hosted default bundle declared five of eight nodes in its only module; three nodes were previously dropped from the Design Map projection. Unassigned nodes now join case-insensitive declared module matches or deterministic derived regions, with an explicit limitation; the default projects Search (6) → catalog (2) and preserves the cross-region graph edge. Projection smoke, build, and diff checks passed. |
| `audit-2026-09-05` | Preserve Lachesis relationship labels | The hosted v2 bundle uses `edge.relation`, while the adapter only read `edge.kind`; cross-region connections rendered without semantic labels. The adapter now accepts and validates both fields, preferring `kind` when both exist, and relation-only projection smoke confirms `call`/`uses` labels survive into the text evidence. Build and diff checks passed. |
| `audit-2026-09-05` | Replay hosted two-region map visually | A production-shaped local build with the hosted default bundle now renders Search (6) → catalog (2) at 375px and 1440px; relationship evidence reads `uses → catalog`, the graph-backed banner remains explicit, and both viewports report zero overflow. Screenshot inspection found no label collisions or invented network geometry. |
| `audit-2026-09-05` | Avoid inferred runtime roles in graph maps | Graph-backed modules without a role field were previously labeled `runtime`, a semantic claim not present in the bundle. They now use the neutral `projected region` label; the Suricata fixture retains explicit entry/runtime/fan-out/output roles. Hosted production replay reports two neutral role labels and zero overflow; detector, build, and diff checks passed. |
| `audit-2026-09-05` | Accept optional graph source metadata | The Lachesis contract permits nodes without `snippet` or `source_window`; the schema guard now accepts those nodes while still rejecting malformed source windows. Projection smoke covers the source-less case, and the UI continues to label unavailable source evidence rather than inventing it. Typecheck, projection smoke, production build, and diff check passed. |
| `audit-2026-09-05` | Cover graph-first bundles without declared modules | The 2.0 contract allows `graph.modules` to be omitted. Design Map now derives deterministic top-level regions from node module/file metadata, preserves node relationships, and discloses the derivation as a limitation. Projection smoke covers the no-module case; typecheck, production build, and diff check passed. |
| `audit-2026-09-05` | Restore Trust handoff touch target | The Trust domain’s contextual Lachesis link had a 36px minimum height despite being a primary mobile action. It now measures 44px at 375px and 1440px with zero overflow; typecheck, production build, detector (intentional map-grid advisory only), and diff check passed. |
| `audit-2026-09-05` | Verify graph evidence wording for single-region projections | Hosted `b_demo1234` contains 7 graph edges but projects to one module. The Architecture map now says those relationships remain within the projected region and are not cross-region connections, instead of incorrectly claiming evidence is absent. At 375px the clarified message rendered with zero overflow; check, production build, detector, and diff check passed. |
| `audit-2026-09-05` | Clarify hosted bundle setup for maintainers | README now includes the exact public Lachesis demo command and deep link, labels the fixture as non-evidence, and explains that omitted bundle API configuration requires a same-origin proxy rather than implying a static host provides one. `npm run check`, projection smoke, and diff check passed. |
| `audit-2026-09-05` | Replay hosted bundle hydration in a production-shaped local run | Built Design Map with `NEXT_PUBLIC_BUNDLE_API_URL=https://lachesis.unboundcompute.com`, started the production server locally, and opened `/architecture?repository=demo%2Fatlas-commerce&revision=main&bundle=b_demo1234` in Playwright. The hosted bundle hydrated successfully, preserved the demo-fixture provenance warning, rendered the bounded one-region projection, and stayed at zero horizontal overflow. No AWS resources were accessed. |
| `93aa5eb` | Complete region chapter orientation | Region chapters now explain why the region exists and identify the representative repository path before the Lachesis handoff, in addition to responsibility, neighbors, inputs/outputs, structures, and anchors. Typecheck, production build, contextual route smoke, and diff checks passed. |
| `audit-2026-09-05` | Inspect the eight-region Start map visually | Full-page Playwright captures at 375px and 1440px show readable node labels, no node collisions, no horizontal overflow, and the separate Initialization/Pattern matching stages remain legible; the ordered text path remains available beneath the canvas. |
| `ec58c71` | Replace stale generated design-system guidance | `design-system/design-map/MASTER.md` now reflects the Luna field-guide tokens, semantic/layout rules, accessibility floor, and explicit rejection patterns instead of the superseded blue dashboard/card direction. `git diff --check` passed. |
| `82ca373` | Include boot and matcher in the Start projection | The compact Start map now shows eight bounded Suricata regions, including separate Initialization and Pattern matching stages; this preserves the boot/runtime distinction and detection fan-out without exceeding the nine-region limit. Desktop/mobile node probes found no overlap or horizontal overflow; route smoke passed. |
| `f6f3f7f` | Lead Start with the repository map | Start now follows the brief’s orientation order: repository thesis → bounded map → ordered macro path → first-flow/Architecture actions → provenance note. At 375px the map begins at `632px` with zero overflow; at 1440px it begins at `676px`. Typecheck, production build, contextual route smoke, and diff checks passed. |
| `audit-2026-09-05` | Recheck the public Lachesis transport boundary | Fresh read-only probes return 200 for the public Explorer root and downloadable demo artifact, but 404 for `GET /api/bundles/b_demo1234`; local opaque transport remains available and the frontend continues to expose an explicit unavailable state rather than substituting fixture facts. |
| `a04760b` | Remove superseded Start order overrides | The Start DOM already matches its map-first composition, so redundant flex `order` rules were removed instead of leaving a CSS-only focus/visual dependency. Typecheck, production build, and diff check passed. |
| `audit-2026-09-05` | Re-run the projection, hosted-state, and Explorer handoff gates after focus-order fix | `npm run projection-smoke`, `npm run hosted-smoke`, and the local `npm run explorer-smoke` all passed; the live integration projected 1 bounded region and 7 graph relationships without changing the mobile focus-order fix. |
| `5c8da0a` | Align Start visual and keyboard order | Start now keeps the DOM order identical to the map-first visual order: heading → map → ordered reading path → caption. Typecheck, diff check, and Playwright probes at 375px/1440px passed with zero horizontal overflow. |
| `33be658` | Fix relationship transcript reading width | The ordered Architecture relationship alternative now places each explanation in the content column instead of the counter column, preventing desktop rows from wrapping one word per line. Computed Playwright layout at 1440px reports a 642px explanation width; the corrected screenshot was inspected, with `npm run check` and `git diff --check` passing. |
| `3dee03f` | Make unavailable-bundle smoke deterministic | The missing graph-backed chapter case now uses an opaque ID that is distinct from the local demo fixture, so the designed unavailable state is verified consistently against either the public API or a locally running Explorer server. Full Design Map route smoke and the Explorer integration smoke passed with the local servers. |
| `audit-2026-09-05` | Re-audit projection and interactive semantics | `npm run projection-smoke` passed the 0/1/8/30/500 community bounds, topology fixtures, alias handling, and malformed-bundle guards. A Playwright sweep across Start, Architecture, Flows, dedicated Flow, Trust, Explore, and Embed at 375px and 1440px found one `h1`/`main`, zero horizontal overflow, no unlabeled visible controls, and no sub-44px targets. |
| `audit-2026-09-05` | Recheck distribution transport recovery | `npm run hosted-smoke` passed the valid, malformed, unavailable, timeout, expiry, oversized, and network recovery cases for bundle loading. The embed route remains a bounded map-only surface, while the full Architecture route owns the README iframe authoring snippet. |
| `audit-2026-09-05` | Replay maintainer embed consumption | Contextful `/embed?repository=demo%2Fatlas-commerce&revision=main&bundle=b_demo1234` returned 200 at 375px and 1440px with zero overflow, no inspector chrome, a visible transport/provenance message, and a repository-specific title/footer. The full Architecture route still exposes the copyable README snippet. |
| `audit-2026-09-05` | Replay no-view Lachesis handoff in Explorer | The full Design Map handoff URL opened the local Explorer, loaded `b_demo1234`, normalized to `view=map`, preserved repository/revision/region/anchor/flow/step/domain context, and rendered the explicit `DESIGN MAP CONTEXT` marker. The Explorer browser surface showed the loaded graph and its bounded relationship count. |
| `732d7b6` | Put the mobile repository map before its linear alternative | At mobile widths, Start now orders `Repository shape` → map → `Ordered reading path`, matching the brief’s dominant-visual wireframe and keeping the first map stages visible at 375px (`top: 772px`) without overflow; 320/375/1440 layout probes and screenshot review passed. |
| `audit-2026-09-05` | Re-run the production build gate after mobile ordering | `npm run build`, local-API route smoke, and `git diff --check` passed after the mobile Start order change; the full 22-route Next build remains clean. At 320px the primary action remains visible and the document has zero horizontal overflow. |
| `audit-2026-09-05` | Re-run the Explorer companion gate | Lachesis Explorer `typecheck`, production `next build`, `git diff --check`, and `LACHESIS_BUNDLE_ID=b_demo1234 node scripts/smoke-browser.mjs http://localhost:3210` all passed. The companion still exposes the expected root page and opaque bundle API route; only its pre-existing local deployment files remain untracked. |
| `fd07d12` | Clarify embed sharing contract | README now distinguishes Copy link controls on full-guide pages from the intentionally clean `/embed` artifact, and explicitly directs maintainers to share the embed URL or copy the Architecture page’s iframe snippet. `git diff --check` passed. |
| `3692f74` | Align local Explorer smoke instructions | The integration smoke default and README now use the verified local Explorer port `3210`; running `npm run explorer-smoke` without an override passed against the running companion server. |
| `audit-2026-09-05` | Recheck public Explorer transport state | The public Explorer homepage and `/code-exploration-bundle.json` both return HTTP 200, while `https://lachesis.unboundcompute.com/api/bundles/b_demo1234` still returns HTTP 404. Local opaque-bundle loading remains verified; public generated-bundle transport is not yet available. |
| `audit-2026-09-05` | Validate hosted service readiness locally | `sam validate --template-file services/bundle-service/template.yaml --region us-east-1 --lint` passed, and the bundle-service contract/worker suite passed all 27 tests. Deployment still requires the immutable worker wheel/image and production AWS inputs documented by the service README. |
| `audit-2026-09-05` | Inspect deployable AWS state | Read-only AWS inspection confirms the deployer identity is valid and the immutable, scan-on-push `lachesis-worker` ECR repository exists, but there are no deployed CloudFormation stacks and no local Lachesis wheel artifact. The hosted gap is therefore an external release-input gap, not an unvalidated frontend contract. |
| `audit-2026-09-05` | Reconcile source-tree terminology | A stale-term sweep found no obsolete implementation vocabulary or old dark-map references; remaining uses of “stale,” “fixture,” and “single-page” are intentional state labels or historical/reference audit notes. Both worktrees contain no new generated tracked-file drift. |
| `70a08bf` | Keep CORS on opaque-bundle recovery responses | Lachesis Explorer now applies `Access-Control-Allow-Origin: *` to valid, malformed, and unknown bundle responses, not only successful payloads; its browser smoke asserts GET recovery statuses and OPTIONS preflight headers. A freshly restarted local server passed the updated smoke. |
| `audit-2026-09-05` | Replay malformed and expired bundle recovery in Design Map | At 375px, malformed `bundle=not-a-bundle` and unknown `bundle=b_missing9999` both render the explicit `UNAVAILABLE` alert, preserve zero overflow, omit fixture content, and expose retry/Start/Lachesis recovery actions. The expired case retains supplied repository/revision context. |
| `445cb8b` | Verify bundle recovery CORS in a real browser | Explorer smoke now accepts `CORS_TEST_ORIGIN` and performs browser `fetch` calls from a separate Design Map origin. The valid, malformed, and unknown bundle responses were all readable with expected statuses across `localhost:3304` → `localhost:3210`. |
| `audit-2026-09-05` | Run the consolidated cross-repo gate | Design Map check, projection smoke, hosted transport smoke, Explorer integration smoke, local route smoke, Explorer typecheck/build/browser smoke, bundle-service tests (27 passed), and all diff checks passed in one clean verification run. Design Map is clean; Explorer retains only its pre-existing local deployment files. |
| `1288d55` | Document the bundle API configuration boundary | README now documents `NEXT_PUBLIC_BUNDLE_API_URL` alongside `NEXT_PUBLIC_LACHESIS_URL`, provides the verified local `3210` dual-origin command, and states the HTTPS/URL-shape production requirements. `git diff --check` passed. |
| `audit-2026-09-05` | Recheck Architecture transcript accessibility after layout fix | The rebuilt Architecture page reports zero overflow, 30 labeled focusable controls, active `:focus-visible` styling, and a 12px relationship explanation using the existing 4.56:1 muted-on-paper contrast pair. The corrected 642px desktop explanation column remains intact. |
| `15a06d8` + `12541e1` | Connect the Explorer demo transport and keep fixture provenance honest | Lachesis Explorer now exposes `GET /api/bundles/b_demo1234` with strict opaque-ID validation, CORS, and an allowlisted schema-valid fixture. Design Map’s configured API URL no longer creates a double slash, and a loaded `meta.fixture: true` bundle is labeled `Demo fixture · not verified` with an explicit non-evidence warning. Explorer `typecheck`, bundle verification, and build plus Design Map `hosted-smoke`, `smoke`, build, and `git diff --check` passed. |
| `2026-09-05` | Add reproducible Explorer → Design Map integration smoke | `npm run explorer-smoke` fetches the local Explorer endpoint, validates it with the real bundle guard, projects its 7 graph relationships into a bounded region, preserves demo-fixture provenance, and checks invalid/unknown/OPTIONS responses. It passed against the production Explorer preview. |
| `2026-09-05` | Make Level-0 layout topology-aware | The Architecture canvas now selects a deterministic pipeline, hub, or mesh arrangement from projected relationship density/degree instead of always forcing the Suricata pipeline positions; the toolbar names the chosen layout and the text relationship view remains authoritative. `npm run check`, `projection-smoke`, `smoke`, `build`, and `git diff --check` passed. |
| `a001f22` | Preserve direct bundle links during hydration | `MapClient` now retains the server-provided `initialBundle` when the client search-param snapshot is temporarily unhydrated, preventing a shared `?bundle=b_…` Architecture URL from remaining stuck in loading without issuing its API request; `check` and production build passed. |
| `2026-09-05` | Document the cross-repo runtime replay | Explorer’s documented browser smoke now runs against port 3210 with `LACHESIS_BUNDLE_ID=b_demo1234`; the Design Map handoff URL and opaque ID are preserved across the map load. The Explorer browser smoke passed on desktop/mobile before this audit update; public deployment remains separate. |
| `2026-09-05` | Verify direct bundle hydration in a production-shaped browser path | With Explorer on `localhost:3210` and Design Map’s development API base on `localhost:3301`, Playwright loaded `/architecture?repository=demo%2Fatlas-commerce&revision=main&bundle=b_demo1234`, observed the API request, rendered the demo-fixture warning, and selected the deterministic pipeline layout with no error state. The `127.0.0.1` variant is intentionally not used for this dev probe because Next’s dev-resource origin guard prevents hydration. |
| `2026-09-05` | Preserve focused state in README embeds | Architecture’s generated iframe now carries the selected `region`, semantic `level`, and `anchor` alongside repository/revision/bundle context, so maintainers embedding a focused map reopen the same reading position; route smoke asserts the escaped snippet contract. |
| `audit-2026-09-05` | Recheck live Lachesis transport | `https://lachesis.unboundcompute.com/code-exploration-bundle.json` still returns HTTP 200 and a valid `lachesis-explorer-bundle` 2.0-shaped artifact (`demo/atlas-commerce`, `main`, 8 graph nodes, 1 module, 7 edges); the opaque probe `/api/bundles/b_demo1234` remains deployment-dependent on the public host, while the local Explorer route is now verified. |
| `audit-2026-09-05` | Final acceptance gate replay | `check`, `projection-smoke`, `hosted-smoke`, `smoke`, `build`, and `git diff --check` all passed after the latest handoff/metadata changes; route matrix still covers canonical, contextual, malformed, redirect, embed, and discovery paths. |
| `audit-2026-09-05` | Re-run the cross-repo built-server gate | Design Map typecheck/projection/build checks passed; Explorer typecheck/schema/build checks passed; `LACHESIS_BUNDLE_ID=b_demo1234 node scripts/smoke-browser.mjs http://localhost:3210` passed against a freshly restarted server. Both the local API/deep link and `LACHESIS_EXPLORER_URL=https://lachesis.unboundcompute.com npm run explorer-smoke` now verify the current hosted demo transport. |
| `audit-2026-09-05` | Verify Explorer preserves Design Map handoff context | Explorer commit `9866a8e` now retains and renders repository, revision, region/label, anchor, flow, step, and trust-domain parameters after hosted bundle hydration; its browser smoke asserts the rewritten URL and visible context rail. The local deep link passed with `b_demo1234`; public deployment remains outstanding. |
| `ca428d8` | Separate mobile Start reading sections | Added a 28px reading break and rule between “Repository shape” and “Ordered reading path” at the mobile breakpoint; Playwright at 375px measured the intended gap, zero overflow, and the rebuilt production preview rendered the updated hierarchy. |
| `7229895` | Bring the Architecture map into the editorial system | Replaced the dark green canvas, green cards, and low-contrast map copy with a warm paper surface, ink relationship lines, signal-colored accents, and readable light nodes/inspector. This removes the brief’s rejected developer-tool costume while preserving bounded topology and text fallback; rebuilt screenshot review, route smoke, typecheck, build, and diff check passed. |
| `audit-2026-09-05` | Label the cross-product handoff boundary | Explorer commit `ab97a39` adds an explicit `DESIGN MAP CONTEXT` marker before preserved handoff fields; the hosted browser smoke asserts the marker and context URL after bundle hydration. This makes the Design Map → Lachesis altitude change legible without relying on a maintainer explanation. |
| `audit-2026-09-05` | Verify no-view focused handoff bootstrap | Explorer commit `c99952c` now infers the map view when a Design Map link carries region/anchor context without Explorer’s internal `view` parameter, focuses a matching bundle node when available, and keeps flow/step/domain context for continuation. The smoke replay uses the actual no-view handoff shape and passed against the local hosted route. |
| `3d96e36` | Keep the Architecture relationship alternative visible | The ordered “projection, in words” summary is now open by default while remaining collapsible, so relationship meaning is present without diagram interaction or color. Fresh rebuilt previews at 375px and 1440px show all eight summary items with zero overflow; route smoke, typecheck, build, and diff check passed. |
| `dfad148` | Make local Lachesis handoff testing reproducible | Design Map now accepts `NEXT_PUBLIC_LACHESIS_URL` for local Explorer runs while defaulting safely to the public HTTPS origin and rejecting credentials/query strings; route smoke supports the override. A built preview on port 3303 emitted `http://localhost:3210/?…` handoff links, and the default public-origin route matrix still passed. |
| `4e53082` | Identify default flow repository in metadata | Dedicated `/flows/packet-decode` now includes the default `Suricata` identity when no repository query is supplied, matching contextual titles on all other canonical routes; server-rendered title, typecheck, smoke, build, and diff checks passed. |
| `audit-2026-09-05` | Verify route title coverage | Server-rendered canonical routes now expose repository-specific titles for Start, Architecture, Flows, Trust, Explore, Embed, region chapters, and dedicated flow pages; no route falls back to a generic product-only title. |
| `audit-2026-09-05` | Review three-product handoff layout | Contextual Explore at 1440px renders three equal 247px boundary columns; at 375px they stack to 339px with zero overflow, one `h1`, and the full Trace explanation visible. Desktop/mobile screenshots were captured and inspected. |
| `e1f7691` | Complete typed Lachesis handoff context | `LachesisHandoff` now models optional flow, step, and trust-domain identifiers in addition to repository, revision, region, anchor, and opaque bundle identity, matching the URL contract already exercised by the UI; typecheck, route smoke, and diff checks passed. |
| `audit-2026-09-05` | Full-route touch-target sweep | Playwright measured every visible link, button, summary, input, and select at 375px across Start, Architecture, region chapter, Flows, dedicated flow, Trust, Explore, and Embed; no rendered control was below 44px in either dimension. |
| `audit-2026-09-05` | Semantic control sweep | Playwright inspected every `button`, link, input, select, and summary on Start, Architecture, Flows, Trust, Explore, and Embed at 375px with no unlabeled controls; the skip link focused and activated `#main-content` successfully. |
| `1237827` | Document Trace boundary for distribution | README now states the Design Map/Lachesis/Trace separation alongside the share/embed guidance, so maintainers arriving through repository documentation receive the same altitude contract as handoff readers. `git diff --check` passed. |
| `3bd823c` | Surface Architecture text alternative | Architecture now shows a concise data-derived `Text view` sentence before the optional relationship transcript, including explicit wire-input and consumer-output boundaries; graph-backed bundles without edge evidence state that no path is inferred. Route smoke, typecheck, build, and diff checks passed. |
| `b132c0d` | Clarify Design Map / Lachesis / Trace boundaries | The Lachesis handoff now names Trace as the destination for reviewed, adjudicated findings, keeping raw suspicion and source evidence out of Design Map; the boundary is protected by route smoke alongside the existing two product descriptions. `npm run check`, `npm run smoke`, `npm run build`, and `git diff --check` passed. |
| `bf1f3cb` | Compact mobile Start path | At 375px the six-stage ordered path now uses a readable two-row grid (118px tall instead of 290px), preserving DOM order, individual deep links, and zero horizontal overflow while bringing the map surface 180px closer to the first viewport; `npm run smoke`, `npm run build`, and `git diff --check` passed. |
| `f3055c6` | Label intro coverage scope | Page-intro provenance rows now prefix the raw scope with `Coverage:`, removing the ambiguous repeated scope text while retaining the snapshot state label; `npm run check`, `npm run smoke`, `npm run build`, and `git diff --check` passed. |
| `178c28a` | Put the single primary journey in the first mobile viewport | The only `Follow a packet` primary action now follows the repository introduction instead of sitting below the architecture preview. Playwright at 375×812 placed it from 724–772px, counted one primary journey action, and reported zero horizontal overflow. |
| `59aa8af` | Restore the region chapter reading layout and target floor | Region metadata, child responsibilities, relationship neighbors, and chapter sections now have an explicit editorial grid/list structure instead of relying on browser-inline defaults. Neighbor links are 52px high; the mobile wordmark reserves a 44px-wide target. Playwright screenshots at 375×812 and 1440×1000 show readable chapter hierarchy, and the full viewport sweep reports zero overflow, one `h1`/`main`, and no visible interactive target below 44px. |
| `dc65006` | Make the mobile flow transcript a direct controller | The mobile “All five handoffs” text mode now exposes five keyboard/touch-selectable rows that share the canonical step state, URL, and narrative with Previous/Next; desktop keeps the spatial path controller. Playwright selected `validated IPv4 payload` from row 3 at 375×812, confirmed `?step=ipv4`, five rows, zero overflow, and targets ≥44px. |
| `496be76` | Prevent architecture map collisions at desktop and tablet widths | The deterministic spatial layout now uses a four-column pipeline with a separated detection branch; the intermediate-width composition switches to an ordered full-width map before fixed node widths can overlap. Rectangle-intersection checks reported zero node collisions at 1024×768 and 1440×1000, and screenshots were inspected at both widths; `npm run check`, `npm run smoke`, and `git diff --check` passed. |
| `4daa994` | Cover the intermediate map breakpoint | The ordered tablet composition now remains active through 1279px; the spatial map resumes at 1280px where its 653px canvas has enough room for the fixed 150px nodes. Boundary checks at 1279 and 1280 reported zero overlaps and zero viewport overflow. |
| `d07ccc9` | Restore required Suricata boot and dispatch vocabulary | Initialization now exposes `SigTableSetup()` and `PostConfLoadedSetup()` without duplicating the primary anchor, the runtime branch explicitly names `StateAlloc`, and Flow + stream state explains why `Packet` and `Flow` connect the pipeline. Level-2 Architecture smoke rendered both boot anchors and the branch copy with zero overflow; `npm run check`, `npm run projection-smoke`, and `git diff --check` passed. |
| `a7cfbe5` | Align runtime dispatch smoke copy | Route smoke now asserts the shipped `StateAlloc selects the parser path from observed flow state.` vocabulary, keeping the verification contract aligned with the reader-facing Suricata explanation. `npm run smoke` passed across the full route matrix. |
| `c375526` | Anchor Start with the interactive architecture map | Start now places the bounded `MapClient` projection beneath its repository orientation, keeps the ordered macro path as the text fallback, and labels each node’s role (including boot/runtime) so topology is not color-dependent. `npm run check`, `npm run smoke`, `npm run build`, and `git diff --check` passed. |
| `1fd45c0` | Protect the Start map contract | Route smoke now requires the Start page’s bounded map, repository-specific node content, explicit role labels, and ordered path fallback in server-rendered HTML. `npm run smoke` passed across the full route matrix. |
| `dd948c8` | Complete the Architecture text alternative | The visible relationship disclosure now names the external wire-input and consumer-output boundaries in addition to the bounded region list, so the architecture remains complete without relying on SVG geometry. `npm run check`, `npm run smoke`, and `git diff --check` passed. |
| `10740da` | Protect the Architecture boundary alternative | Route smoke now asserts the external-boundary sentence in the server-rendered relationship alternative. `npm run smoke` passed across the full route matrix. |
| `065fc96` | Keep hosted snapshot context synchronized | Hosted map loading now refreshes its pending repository/revision identity when those shared URL fields change, while avoiding refetches for ordinary region/level selection changes. `npm run check`, `npm run smoke`, and `git diff --check` passed. |
| `8db2b4e` | Keep the Start map narrative complete | Start no longer repeats the structural thesis and its compact six-region projection deliberately preserves input, decode, flow state, protocols, detection, and output. The full Architecture route retains the complete region set. Browser geometry at 375px reports zero overflow and 534px map height. |
| `2da262e` | Protect Start map stages | Route smoke now requires both `Packet input` and `Alerts + output` in the Start map, alongside its bounded map and ordered fallback. `npm run smoke` passed across the full route matrix. |
| `2fe9778` | Tighten Start orientation rhythm | Removed the repeated thesis section and shortened the map introduction; at 375px the action block ends at 725px and the interactive map begins at 794px with zero horizontal overflow, bringing the visual anchor immediately after the first decision. |
| `ffff089` | Align Start heading contract | Route smoke now asserts the intentionally concise `Repository shape` heading. `npm run smoke` passed across the full route matrix. |
| `0f68b53` | Bring Start map into the mobile viewport | Mobile action padding, heading scale, and map spacing now place the interactive map at y≈765px in a 375×812 viewport; the toolbar and first stage are visible immediately after the recommendation, with zero horizontal overflow. `npm run check`, `npm run smoke`, and `git diff --check` passed. |
| `f189d0b` | Prioritize the mobile macro path | Start’s mobile composition now orders the readable macro path before the deeper interactive map, matching the responsive brief while preserving the desktop map-first hierarchy. At 375px the path begins at y≈799px with zero overflow; `npm run check`, `npm run smoke`, and `git diff --check` passed. |
| `6bd7b75` | Preserve mobile Start reading order | The map caption now follows the ordered path and interactive map in the mobile flex composition instead of jumping above the section heading. Browser inspection at 375px shows `Repository shape` → `Ordered reading path` → description, with zero overflow; `npm run check`, `npm run smoke`, and `git diff --check` passed. |
| `51ad46d` | Align Start DOM and visual order | Start’s DOM now follows the mobile reading sequence (`Repository shape` → ordered path → map → caption), while desktop flex ordering restores map-first presentation. Browser geometry at 375px and 1440px reports zero overflow; `npm run check`, `npm run smoke`, and `git diff --check` passed. |
| `2026-09-05` | Verify Start breakpoint geometry after DOM reorder | Browser measurements at 375, 768, 1024, 1279, 1280, and 1440px report zero horizontal overflow; mobile keeps the ordered path before the map, while 1024px+ restores map-first ordering and the existing tablet map composition. |
| `2026-09-05` | Recheck production interaction contracts | A fresh `next start` browser run at 375px confirmed Architecture node selection navigates to `/architecture/input?region=input&level=1`, the mobile Flow transcript is open by default and row 3 sets `?step=ipv4`, and Trust search sets `?q=memory` with 3 matching domains; overflow remained zero. |
| `2026-09-05` | Recheck semantic route structure | Browser inspection of all canonical routes found exactly one `main`, no duplicate IDs, zero horizontal overflow, one logical `h1`, and a nested heading sequence that keeps map, flow, trust, chapter, and handoff sections distinguishable. |
| `856c2e0` | Enforce unique route IDs | Route smoke now rejects duplicate `id` attributes in every server-rendered route, turning the semantic audit’s no-duplicate-ID check into a release gate. `npm run smoke` passed across the full route matrix. |
| `eee1694` | Distill Architecture, Flows, embed, and mobile provenance | Architecture’s text projection is a closed disclosure, its mobile inspector is omitted in favor of the existing region-chapter navigation, Flows has one direct step selector per viewport, and `/embed` renders only the map rather than a full mini-application. The repository bar now gives provenance a non-truncated second row on mobile. Playwright verified zero overflow, the desktop/mobile flow controller switch, working Next/map selection URLs, a closed Architecture disclosure, and no embed inspector/relationship summary at 1440×1000 and 375×812; `npm run check`, `npm run projection-smoke`, `npm run hosted-smoke`, `npm run smoke`, `npm run build`, and `git diff --check` passed. |
| `14569aa` | Remove the duplicate Architecture region directory | The map and its bounded textual alternative remain the authoritative region index; the second directory rendering of the same projection was deleted. Route/build checks passed and Playwright found no `.region-directory` on Architecture at desktop or mobile widths. |
| `b57831c` | Reduce Start here to one orientation and one decision | Removed the repeated CTA pair and the three-link “Choose a way in” directory while preserving the repository thesis, macro path, contextual URLs, provenance, and one primary/secondary action pair. Route, build, and type checks passed. |
| `2026-09-05` | Verify coverage-limited graph state at the rendered boundary | A Playwright run with two valid projected regions, `indexed_nodes: 20`, `included_nodes: 2`, and a generated-slice limitation rendered `Graph-backed · coverage limited` plus the plain-language note that the view covers only part of the indexed repository. |
| `2026-09-05` | Verify stale graph-backed state at the rendered boundary | A Playwright run with a valid mocked bundle carrying `stale snapshot; regenerate before relying on it` rendered `Graph-backed · stale snapshot`, the explanatory stale-copy note, and the explicit no-relationship-evidence state on Architecture. |
| `1c12402` | Raise trust vocabulary labels | Trust domain metadata, family terms, and flow guard labels now explicitly use 12px when they are reader-facing vocabulary; compact code identifiers remain unchanged. The prior computed-style sweep identified these concrete sub-12px violations, and `npm run check`, `npm run smoke`, `npm run build`, and `git diff --check` passed after the fix. |
| `52c8212` | Preserve incoming relationship evidence | The typed projection now carries edge-kind labels in both directions, so an inspector can distinguish evidence received from an upstream region as well as evidence handed off downstream. Projection smoke asserts both maps; Playwright mocked-bundle runs rendered `Relationship evidence: calls → Detect.` and, when focused on Detect, `Relationship evidence: calls ← Decode.`; `npm run check`, `npm run projection-smoke`, `npm run smoke`, `npm run build`, and `git diff --check` passed. |
| `b48f15b` | Raise Architecture metadata type size | The map’s node metrics, focus label, and region-directory counts now declare 12px at their base selectors, matching the existing informational floor even outside the shared override list. A 375px Playwright check reported `12px` for node metrics; `npm run check`, `npm run smoke`, `npm run build`, and `git diff --check` passed. |
| `3060b71` | Surface relationship evidence labels | The Architecture map now exposes projected edge-kind labels in the ordered reading surface when a graph-backed snapshot supplies them, keeping “hands off to” direction separate from evidence such as `call`; the note is absent for illustrative snapshots. A Playwright run with a valid mocked Lachesis bundle rendered `Relationship evidence labels: calls.` at `/architecture?bundle=…`; `npm run check`, `npm run smoke`, `npm run build`, and `git diff --check` passed. |
| `2026-09-05` | Verify 200% zoom reflow | Playwright applied a 200% document zoom to `/`, `/architecture`, `/flows`, `/trust`, `/explore`, and `/embed` at a 1280×900 viewport; every route retained zero horizontal overflow, confirming the reflow requirement for enlarged text. |
| `c8badc1` | Preserve relationship-kind provenance | Region projection now carries deterministic edge-kind labels (such as `call`) alongside each validated upstream/downstream pair, so future readers and embeds can distinguish a relationship’s direction from its evidence type without changing the bounded map shape. Projection smoke asserts the label, with `npm run check`, `npm run projection-smoke`, `npm run hosted-smoke`, `npm run smoke`, `npm run build`, and `git diff --check` passing. |
| `7b98885` | Test the hosted body-size cap without headers | Hosted transport smoke now verifies an oversized response body is rejected even when `content-length` is absent, covering the byte-count guard as well as the declared-size guard; `npm run hosted-smoke`, `npm run check`, and `git diff --check` passed. |
| `9880dbd` | Require readable graph-node source representations | The Lachesis boundary now requires each node to provide non-empty `snippet` or `source_window` content, matching the 2.0 contract while keeping source bodies out of the HLD UI; projection smoke covers both valid forms and missing/empty representations, with `npm run check`, `npm run projection-smoke`, `npm run hosted-smoke`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `a853ed2` | Restrict source URL templates to HTTP(S) | Optional Lachesis `meta.source_url_template` values now reject non-web schemes before entering the validated snapshot boundary; projection smoke covers a `javascript:` regression, with `npm run check`, `npm run projection-smoke`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `3fcc24c` | Cover hosted network failures | Hosted transport smoke now asserts a pre-response network exception becomes the reader-facing “could not be reached” error, completing the unavailable-state branch alongside read, JSON, expiry, and size failures; `npm run hosted-smoke`, `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. |
| `ff6461e` | Publish the hosted transport gate | README’s pre-share verification sequence now includes `npm run hosted-smoke`, making the transport boundary test discoverable alongside projection, build, and route checks; `git diff --check` passed. |
| `27f217d` | Normalize hosted response read failures | Hosted bundle loading now translates stream/body-read exceptions into a recoverable reader-facing message; `hosted-smoke` covers the failure alongside malformed JSON, expiry, size, and opaque-ID cases, with `npm run hosted-smoke`, `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `c72a289` | Add focused hosted transport smoke | New `npm run hosted-smoke` coverage verifies opaque-ID rejection before fetch, JSON/no-redirect request options, successful parsing, expired bundles, malformed responses, and the 25 MiB size limit; `npm run hosted-smoke`, `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. |
| `e5764a7` | Reject conflicting node module membership | Projection now declines to infer relationships when a node’s direct `module` field conflicts with `modules[].node_ids`, preserving provenance instead of silently choosing one source; projection smoke covers the conflict case, with `npm run check`, `npm run projection-smoke`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `e945dab` | Assert deterministic footprint ranking | Projection smoke now runs the same snapshot twice and verifies stable output plus node-count ranking, covering the scalability requirement that identical graph snapshots retain identical ordering and positions; `npm run projection-smoke`, `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. |
| `461f4b5` | Prioritize canonical module IDs over aliases | Relationship projection now resolves exact module IDs before display-name/path aliases, so a colliding name cannot shadow an authoritative exporter ID; projection smoke covers canonical-ID precedence, with `npm run check`, `npm run projection-smoke`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `c5c7fbf` | Keep integration documentation current | README now describes the adapter’s validated module-level relationship projection alongside identity, coverage, and limitations, matching the shipped Lachesis boundary. `git diff --check` passed. |
| `f46676e` | Validate graph capability metadata | The Lachesis 2.0 adapter now validates optional `graph.capabilities` as a string array instead of silently accepting malformed capability declarations; projection smoke covers invalid shapes, with `npm run check`, `npm run projection-smoke`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `b183cf5` | Keep ambiguous module aliases out of relationships | Alias resolution now records collisions across module IDs, names, or paths and declines to infer a relationship when a node’s module reference is ambiguous; projection smoke covers the collision case, with `npm run check`, `npm run projection-smoke`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `80806d9` | Resolve exporter module aliases in relationship projection | Node relationships now resolve module IDs, names, and paths to the canonical module hierarchy before mapping edges to regions; projection smoke covers nodes that identify modules by name without `node_ids`, with `npm run check`, `npm run projection-smoke`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `d37e4cd` | Cover pipeline, fan-out, and flat-mesh projections | Projection smoke now exercises the topology families named by the scalability rules and asserts deterministic upstream/downstream preservation for each; `npm run projection-smoke`, `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. |
| `c112e00` | Deduplicate bidirectional chapter neighbors | Region chapters now render one neighbor row per region while preserving combined `upstream + downstream` direction labels, preventing duplicate links for bidirectional relationships; `npm run check`, `npm run smoke`, `npm run build`, and `git diff --check` passed. |
| `9360bdd` | Expose direct relationships in the map inspector | The selected-region inspector now states semantic `receives from` and `hands off to` relationships alongside footprint, path, and anchor facts; graph-backed snapshots say when relationship evidence is unavailable, and route smoke protects the Architecture labels with `npm run check`, `npm run smoke`, and `git diff --check` passing. |
| `4343ef9` | Reject inconsistent coverage metadata | Bundle validation now requires `coverage.included_nodes` to equal the actual graph node count and `coverage.indexed_nodes` to cover that included set, matching the Lachesis 2.0 contract; projection smoke covers mismatched and inverted counts, with `npm run check`, `npm run projection-smoke`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `8b9f864` | Validate graph edge identities | Optional Lachesis edge IDs are now typed, required to be non-empty when supplied, and rejected when duplicated; projection smoke covers blank and duplicate edge IDs, with `npm run check`, `npm run projection-smoke`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `b9ec884` | Validate graph node hierarchy references | Bundle nodes now support the Lachesis `parent_id` field and reject blank, dangling, self-referential, or cyclic parent chains before projection; projection smoke covers all four regressions, with `npm run check`, `npm run projection-smoke`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `bb1d312` | Align bundle validation with the Lachesis 2.0 contract | The adapter now requires a non-empty graph, non-empty identity fields, valid edge/module/node references, unique module membership, and an acyclic module hierarchy; projection smoke covers empty, blank, duplicate, dangling, and cyclic regressions, with `npm run check`, `npm run projection-smoke`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `6ae8c34` | Project validated Lachesis graph relationships | The 2.0 adapter now accepts optional `graph.edges`, validates every endpoint, maps node edges through module ancestry into deterministic top-level `upstream`/`downstream` relationships, and folds omitted modules into the nine-region remainder; absent edges still produce an explicit unavailable state. Projection smoke covers populated and dangling-edge cases, with `npm run check`, `npm run projection-smoke`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `948b413` | Enforce the nine-region macro projection | Hosted architecture and region-chapter projections now apply the required nine-region bound at the adapter call site, so a tenth or later top-level module becomes an explicit deterministic remainder instead of being silently omitted after rendering; projection smoke covers 10 → 9 with a roll-up, and `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. |
| `b139414` | Reject dangling graph references | The Lachesis adapter now rejects duplicate node/module IDs, module references to missing nodes, and missing parent modules before projection; malformed-reference regressions are covered in `projection-smoke`, with `npm run check`, `npm run projection-smoke`, `npm run build`, and `git diff --check` passing. |
| `0726e5f` | Remove stale bundle links on client reset | Generated inspector, region, and handoff links now apply the same server-only `initialBundle` fallback as bundle loading, so navigating client-side to a URL without `bundle` cannot keep emitting links for the old graph; `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. A browser transition from `bundle=b_demo1234` to the same route without `bundle` found zero stale bundle links. |
| `725b328` | Clear the initial bundle fallback on URL removal | Bundle selection now uses `initialBundle` only during server render; on the client, a missing `bundle` query value resolves to the illustrative route instead of reusing the original prop, so shared navigation cannot retain an old graph under a bare URL. `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. |
| `5e7e46d` | Publish provenance during bundle transitions | Bundle loading now publishes an explicit pending snapshot when a shared bundle changes and restores the illustrative snapshot when the bundle is removed, so live rail/badge metadata cannot remain on a previously loaded graph during the transition; `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. |
| `8c05a09` | Reload hosted bundles when shared context changes | `MapClient` now keys hosted loading to the current `bundle` query value, aborts stale requests, and resets to the illustrative state when the bundle is removed, preventing an old graph from remaining under a new shared URL; `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. |
| `2026-09-05` | Re-verify responsive and landmark invariants after live provenance updates | Playwright swept `/`, `/architecture`, `/flows`, `/trust`, `/explore`, and `/embed` at 375×812, 768×1024, 1024×768, and 1440×1000: every surface had zero horizontal overflow, exactly one `h1` and one `main`, and no visible link/button/summary/input/select below 44px in either axis. |
| `a430577` | Remove superseded snapshot badge implementation | Deleted the unreachable server-only `SnapshotState` after the live provenance islands replaced it; route smoke now checks the shell source for the obsolete export, with `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `08403de` | Sync authoritative repository identity after validation | The repository bar now updates owner/name, short revision, and full revision from the validated bundle rather than retaining query identity; Playwright mocked `Owner/Repo` at `abcdef1234567890` and confirmed the bar, revision disclosure, rail metadata, indexed count, and both provenance badges all switched together. Route smoke and `npm run check`, `npm run build`, `git diff --check` passed. |
| `f0b5fbd` | Sync repository rail metadata after bundle validation | The persistent rail now updates revision, generation time, coverage scope, indexed-node count, and explanatory provenance note from the same small live snapshot payload as the status badge; Playwright with a valid mocked bundle confirmed `main`, `2026-09-05T00:00:00Z`, `generated slice`, and `1 nodes` replaced fixture metadata, while route smoke and fixture-free failure checks passed with `npm run check`, `npm run build`, and `git diff --check`. |
| `4ea57f8` | Sync repository provenance after bundle validation | The Architecture shell now receives a small client-side snapshot-status update when `MapClient` validates a hosted bundle, so the repository bar and page source change from “bundle requested” to graph-backed sparse/limited/verified state without serializing the illustrative snapshot into failed chapter HTML. Playwright with a valid mocked bundle (`b_demo1234`) confirmed both shell badges became `Graph-backed · sparse projection`; a failed bundle chapter remained fixture-free, and `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. |
| `f6fd1d3` | Reject null coverage objects | The Lachesis bundle guard now treats `coverage: null` as malformed without dereferencing it; projection smoke covers the regression and `npm run check`, `npm run projection-smoke`, `npm run build`, and `git diff --check` pass. |
| `64e7766` | Reject null hosted-bundle metadata | Optional bundle fields now distinguish omitted values from malformed `null` values across timestamps, descriptions, node metadata, module metadata, and coverage metadata; projection smoke adds null regressions alongside the existing malformed cases, with `npm run check`, `npm run projection-smoke`, `npm run build`, and `git diff --check` passing. |
| `1a661be` | Add a synchronized flow path overview | Guided Flows now include a compact semantic path overview sourced from the canonical steps; the current stage and its two neighbors are visibly marked, each stage is keyboard-selectable, and mobile hides the spatial strip in favor of the linear fallback. Playwright confirmed one current/two adjacent stages at desktop, step selection updates URL and narrative, and 375px mobile has zero overflow; `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. |
| `b8bde0a` | Preserve bundle identity in dedicated flow previews | `/flows/[flow]` metadata now carries repository, revision, and opaque bundle context into its Open Graph/Twitter image URL; route smoke asserts the bundle-qualified preview on `/flows/packet-decode`, with `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `3c8ffaa` | Make architectural dispatch branches selectable | The Flows branch explanation now exposes keyboard-operable Static protocol and Runtime flow choices with `aria-pressed`, a live explanatory region, and a URL-backed `branch` state; Playwright at 375px confirmed the runtime choice updates the URL/content with zero overflow, while `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. |
| `83a5a29` | Expose the Lachesis handoff in discovery | The documented `/explore` distribution surface is now included in `sitemap.xml`; route smoke uses a discovery-specific XML assertion (rather than document-landmark checks) and confirms `<loc>http://localhost:3000/explore</loc>`, with `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `d526d09` | Include verified chapter anchors in shell handoffs | Region chapters now enrich the shared handoff context with their projected label and primary anchor even when the URL did not include `anchor`; route smoke asserts the Suricata decode chapter’s complete Lachesis URL, with `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `84098f2` | Preserve complete fallback handoff context | Selection-only links now use the current snapshot’s repository/revision defaults instead of falling back to the Lachesis homepage or emitting an incomplete URL; route smoke covers `/explore?region=decode&anchor=DecodeIPV4%28%29`, with `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `ddd3f52` | Preserve deep-link selection in the shell handoff | Shared context now carries optional region/label/anchor/flow/step/domain fields, and the persistent Lachesis link includes them on deep-linked pages while primary rail links keep only repository context; route smoke asserts a focused Architecture handoff URL, with `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `1aecb31` | Give map error and empty states explicit recovery paths | Failed bundle states now offer retry, Start here, and a context-preserving Lachesis route; valid empty projections offer Start here and Lachesis links instead of mentioning recovery only in prose. Playwright mocked a failed bundle at 375px and confirmed the error, both recovery links, and zero overflow; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `bcc8970` | Announce selected Trust domains without color | Deep-linked Trust articles now include an explicit screen-reader “Selected trust domain” signal in addition to the visual state; route smoke asserts the accessible marker, with `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `b4588b3` | Validate optional graph coverage metadata | Coverage scope, counts, limitations, and capabilities now require their declared types before graph-backed projection; projection smoke rejects malformed arrays and fractional counts, with `npm run check`, `npm run projection-smoke`, `npm run build`, and `git diff --check` passing. |
| `ea88c9f` | Validate graph node and module identity fields | The Lachesis schema guard now requires node `kind`, validates optional node metadata, and ensures module paths/parents/node IDs have the declared string types; projection smoke rejects malformed node kinds and module IDs, with `npm run check`, `npm run projection-smoke`, `npm run build`, and `git diff --check` passing. |
| `483f687` | Harden the verified-bundle boundary | Lachesis bundles now require language, non-negative integer line/count metadata, and integer node lines; malformed timestamps and counts are rejected. `npm run projection-smoke` now exercises valid and malformed bundles, alongside `npm run check`, `npm run build`, and `git diff --check`. |
| `97c1928` | Keep graph-backed Architecture provenance honest | Architecture’s page-level evidence note now distinguishes a requested graph-backed bundle from the illustrative fixture, avoiding contradictory copy while the client validates the snapshot; route smoke asserts the graph-aware note and rejects the old fixture sentence, with `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `06d811a` | Preserve repository context from the shell handoff | The persistent Lachesis link now carries repository, revision, and opaque bundle parameters when available instead of always opening the explorer homepage; route smoke asserts the encoded contextual URL, with `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `5a25e8d` | Raise remaining mobile metadata labels to the type floor | Mobile `intro-source` no longer overrides its reader-facing provenance/status text to 10px; share controls, snapshot labels, embed labels, map state, and action labels now resolve to 12px while identifiers/decorative indices remain compact. Playwright confirmed 12px computed type and zero overflow on Start, Architecture, Flows, Trust, and Embed at 375px; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `c3da096` | Bound dense map connections | The architecture canvas now renders at most 12 deterministic connections, announces when additional edges are omitted, and points readers to the complete ordered relationship summary; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `86f0a7c` | Make Trust domain deep links real | `/trust?domain=memory-safety` now preserves a selected-domain state, scrolls to the matching glossary article, labels it semantically, and highlights it without relying on color alone; route smoke covers the stable article/title IDs, with `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `d8df4b0` | Keep projected region links URL-safe | Hosted module IDs are now encoded whenever they become `/architecture/[region]` paths, including map selection, inspector, directory, mobile recovery, and chapter neighbor links; this prevents slash/space/colon IDs from breaking deep links. `npm run check`, `npm run build`, and `git diff --check` passed. |
| `76e3048` | Preserve focused state in embeds on first render | `/embed` now passes `region`, `level`, and `anchor` into the server-rendered `MapClient` initial state instead of waiting for hydration; route smoke covers a level-2 focused embed URL, with `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `1c76f6d` | Keep social previews tied to repository context | `documentMetadata` now passes repository/revision into `/opengraph-image` for Start, Architecture, region, Flows, Trust, Embed, and Lachesis handoff routes; route smoke asserts the contextual image URL, with `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passing. |
| `02c8b9b` | Run the Impeccable implementation detector | Detector returned one advisory for `.map-grid`'s two-axis line field. Verified as an intentional map-canvas measurement surface (the design system explicitly permits grid only inside the map), not a page-wide generated-UI pattern; no fix warranted. |
| `ff4c4e1` | Carry snapshot generation provenance | Validated Lachesis metadata now preserves `generated_at` through the HLD projection and exposes it as a semantic `<time>` in the repository rail; malformed non-string timestamps are rejected, while missing timestamps remain explicitly labelled `Not supplied by snapshot`; `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. |
| `69f5ae8` | Raise actionable map and handoff labels to the type floor | Toolbar/banner status, flow warnings, inspector links, map handoff links, and Trust handoff links now meet the 12px informational minimum while compact code snippets and decorative indices remain intentionally dense; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `2420124` | Unify focus rings for native disclosures | Revision and Flow `<summary>` controls now receive the same 3px visible focus treatment as links/buttons; Playwright confirmed both disclosure summaries report a 3px outline, with `npm run check`, `npm run build`, and `git diff --check` passing. |
| `084aa22` | Contain long repository context on mobile | Long `repository`/`revision` URLs now keep the mobile repository bar and open revision disclosure within the viewport; Playwright confirmed zero overflow at 375px in both states, with `npm run check`, `npm run build`, and `git diff --check` passing. |
| `204256f` | Raise visible section labels to the type floor | Repository rail headings, page labels, evidence labels, flow labels, and handoff labels now inherit the 12px informational minimum alongside metadata; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `61b4954` | Make the global skip target keyboard-focusable | The shell’s `main#main-content` now accepts focus, so activating `Skip to content` moves keyboard focus to the document landmark instead of only changing the URL hash; Playwright confirmed `document.activeElement.id === "main-content"`, with `npm run check` and `git diff --check` passing. |
| `9e3bb06` | Raise visible informational text to the 12px floor | Reader-facing revision, provenance, coverage, map labels, flow metadata, trust filters, and handoff metadata now override legacy 10–11px declarations to meet the brief’s minimum informational type size; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `ca53a73` | Disclose full revision context accessibly | The repository bar now shortens long revisions while exposing the complete value in a keyboard-operable disclosure; the full revision remains in the reading rail, and route smoke asserts the disclosure label; `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. |
| `d5cb889` | Personalize share preview images | `/opengraph-image` now accepts repository/revision query context and renders it into the share card, while retaining a safe generic default for direct image requests; the route smoke verifies a contextual PNG response, with `npm run check`, `npm run build`, and `git diff --check` passing. |
| `04b55dd` | Keep graph-backed region previews out of fixture SEO | Region metadata now uses a generic verified-snapshot description whenever `bundle=…` is present instead of leaking an illustrative region summary; route smoke asserts the generic Zeek preview title and all gates passed. |
| `e0f5b05` | Reject fixture fallback in graph chapter smoke | Route smoke now asserts that an unavailable bundle chapter contains neither fixture headings nor fixture structure facts, making the provenance boundary executable; live `npm run smoke` passed. |
| `6d68738` | Surface sparse graph projections explicitly | The repository shell now labels a valid graph-backed snapshot with one or zero regions as `Graph-backed · sparse projection` and explains the limited architectural confidence in the reading rail; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `5a3e946` | Clarify current-projection region recovery | Unknown region chapters now describe the active projection rather than incorrectly calling every missing region “illustrative”; hosted chapter projection also normalizes the bundle once instead of repeating the adapter work; `npm run check` and `git diff --check` passed. |
| `54c7fc2` | Keep graph-backed region chapters provenance-safe | Region chapters now load and project a validated hosted bundle when supplied, derive neighbors from that snapshot, and render a recoverable alert instead of Suricata fixture content when loading fails; live smoke covers the unavailable-bundle chapter path, with `npm run check`, `npm run build`, and `git diff --check` passing. |
| `6658bcf` | Protect complete region chapter content in route smoke | The live route gate now checks `/architecture/decode` for the inputs/outputs section, the `Validated payload window` structure fact, and the `Key structures and state` heading; `npm run smoke` passed against the local dev server. |
| `3eed1e6` | Explain flow branching without a path tangle | The guided flow now names the static EtherType dispatch alternatives and the later runtime flow/parser dispatch separately, preserving the readable canonical path while documenting supported branches; responsive styles stack the explanation on small screens; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `80bb199` | Complete region chapter evidence sections | Region chapters now show explicit inputs, outputs, key structures/state, and honest projection fallbacks; illustrative Suricata regions carry substantive Packet/Flow/parser/detection/output evidence while graph projections preserve optional fields; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `68b2f46` | Clarify internal start navigation | The homepage’s Architecture action now uses the same directional cue as other internal links, reserving the external-tab cue for Lachesis handoffs; `npm run check` and `git diff --check` passed. |
| `dfb5c53` | Make the recommended first read explicit | Start here now places a labelled `Start with` decision immediately after the repository path, with context-preserving `Follow a packet` and `Explore architecture` actions; the homepage route smoke asserts both actions and `npm run check`, `npm run build`, and `git diff --check` passed. |
| `76a1663` | Add descriptive social-preview image alternatives | Shared document metadata and root metadata now expose meaningful `og:image:alt` and Twitter image alt text derived from the page title, improving link-preview context for distribution and assistive tooling; `npm run check`, `npm run build`, `npm run smoke`, and `git diff --check` passed. |
| `1206ab1` | Next app scaffold, HLD shell, map fixture, three lenses | `npm run build` passed |
| `d017257` | README, ignore rules, lockfile and TypeScript config normalization | clean repository after commit |
| `f6a171e` | Responsive map path for small screens | Playwright: `390px` viewport, `scrollWidth - clientWidth = 0` |
| `3e61dd6` | Pin Turbopack to this repository | `npm run build` passed without workspace-root warning |
| `a93f664` | Context-preserving Lachesis handoff | Playwright verified generated `repo`, `commit`, `focus`, and `anchor` query parameters |
| `2efa208` | Distinct System, Data Flow, and Trust Surface reading models plus newcomer guidance | `npm run check`, `npm run build`, Playwright verified lens-specific helper text and labels at mobile width |
| `1564feb` | First-viewport reading rail and explicit “Start with the system map” CTA | Playwright verified CTA anchor navigation, three rail steps, and zero mobile overflow |
| `f8207d5` | Point-of-reading provenance status and expandable evidence explanation | Playwright verified `Graph-backed` status, disclosure expansion, evidence copy, and zero mobile overflow |
| `a053424` | Typed Lachesis graph-first bundle adapter | `npm run check` and `npm run build`; adapter preserves indexed-vs-included counts and projected-subset limitations |
| `5da2f52` | Safe opaque hosted-bundle loader | `npm run check` and `npm run build`; loader validates IDs, rejects redirects, caps body size, and handles expiry/malformed JSON |
| `4b55c54` | Transparent hosted loading, identity, coverage, and error states in the HLD page | Playwright verified invalid bundle IDs produce an accessible alert, default pages remain fixture-ready, and mobile overflow stays at zero |
| `7fe9963` | Deterministic bounded projection from Lachesis modules to HLD regions | `npm run check` and `npm run build`; projection ranks top-level modules by footprint and emits an explicit remainder roll-up |
| `4d07d04` | Render projected hosted region labels and footprints in the HLD canvas | Playwright fulfilled a mocked graph-first bundle and verified live repository/revision metadata, three projected region labels, and module footprint text |
| `9952590` | Capture product context and persist the UI/UX Pro Max design system | PRODUCT.md records audience, boundaries, evidence, and accessibility commitments; generated MASTER.md records typography, palette, spacing, and anti-pattern checks |
| `48730c0` | Split the single dashboard into documentation routes with a human-authored reading rail | `npm run check`, `npm run build`, and local HTTP smoke tests returned 200 for `/`, `/map`, `/flow`, `/trust`, and `/explore`; Impeccable detector findings were reduced to the permitted map-canvas grid advisory |
| `06047cf` | Preserve hosted-bundle loading, error, and graph-backed status in the new map surface | `npm run check` and `npm run build` passed; map client still validates the hosted transport through the existing adapter and announces loading/error/ready states |
| `795c946` | Add a scalable region directory beneath the bounded spatial projection | `npm run check` and `npm run build` passed; hosted projections can expose all bounded regions in a readable list while the canvas stays limited to four placed regions |
| `1a7f60e` | Keep the spatial canvas on its newcomer-safe fixture when a hosted projection is too sparse to place | `npm run check` and `npm run build` passed; empty or single-region projections no longer leave the inspector without a selected region |
| `0351079` | Document the multipage route model in the repository handoff | README now names all five routes and the map/explorer boundary |
| `dc113df` | Audit the current UI/UX and write Luna's replacement implementation goal | `LUNA_UI_UX_REBUILD_GOAL.md` records a 5-dimension 8/20 audit, 14 prioritized findings, a phased commit plan, and measurable acceptance criteria; `npm run check` and `npm run build` passed before the report was committed |
| `a2d636e` | Establish honest repository view models and isolate illustrative Suricata content | `npm run check` and `npm run build` passed; `RepositorySnapshotView` carries provenance/coverage/limitations, `illustrative-suricata.ts` is explicitly non-live, and hosted map handoff uses the same typed snapshot context |
| `624f24e` | Replace the competing CSS layers with one warm technical editorial visual system | `DESIGN.md` defines Read-mode layout/type/tokens/interaction gates; legacy CSS was removed, mobile nodes now use a verified single-column flow, focus and touch sizing were added, and detector findings were reduced to the permitted map grid advisory after the font finding was corrected |
| `1d3b288` | Give Data Flow a distinct ordered reading surface | `FlowDiagram` renders four data nouns, handoffs, guards, region ownership, and symbol-specific Lachesis links; `npm run check` and `npm run build` passed |
| `3a96cbb` | Give Trust Surface its own glossary/index | `TrustGlossary` renders source/guard/sink kinds, plain-language obligations, repository locations, and anchor-specific Lachesis links; `npm run check` and `npm run build` passed |
| `111943b` | Replace Luna's incremental UI plan with a researched, from-scratch implementation specification | The brief defines product boundaries, authoritative inputs, 12 official online references, canonical routes, page-level interaction contracts, semantic zoom limits, responsive and accessible alternatives, Suricata proof content, staged commits, end-to-end tasks, acceptance criteria, and automatic rejection conditions; `git diff --check` passed before commit |
| `158a661` | Record the researched UI references and frontend teardown/retain decision | `UI_REFERENCE_NOTES.md` links the official references, names the observed interaction pattern, records the Design Map decision, and separates replaceable presentation from retained adapters; `git diff --check` passed |
| `1ed525c` | Establish the canonical repository reading architecture | `/` is repository-first Start here; `/architecture` and `/flows` are canonical pages; `/map` and `/flow` redirect; navigation is centralized in the new shell; Explore is explicitly a handoff confirmation surface; `npm run check` and `npm run build` passed |
| `bed786a` | Replace the documentation visual system | Replaced the global stylesheet and route composition with a single light technical-editorial world, one repository shell, no duplicated tabs, map/list mobile reflow, semantic landmarks/focus styles, and route-specific document sections; `npm run check`, `npm run build`, and `git diff --check` passed |
| `6da06d1` | Build the bounded, data-driven Architecture surface | Suricata fixture now has eight meaningful regions with roles, relationships, children, anchors, and a separate boot path; the map draws metadata-backed edges, has a visible text relationship summary, supports `/architecture/[region]`, preserves hosted loading, and switches to ordered content on mobile; `npm run check`, `npm run build`, and `git diff --check` passed |
| `389c404` | Build guided architectural flows | Flow steps now describe the Suricata decode handoffs with data nouns, decisions, guards, anchors, synchronized progress state, URL step deep links, Previous/Next controls, and an accessible all-steps fallback; `npm run check`, `npm run build`, and `git diff --check` passed |
| `eb33ce3` | Build the trust surface glossary/index | Trust now uses eight explicit domains with meaning, obligation, constructor families, locations, search/filter controls, deep links, and non-finding language; `npm run check`, `npm run build`, and `git diff --check` passed |
| `e64d057` | Preserve selected context into Lachesis | `/explore` now parses incoming repository/revision/region/label/anchor/flow/domain/bundle context, renders a confirmation summary, and builds a fixed-origin Lachesis URL without dropping context; `npm run check`, `npm run build`, and `git diff --check` passed |
| `b10f86c` | Resolve the Impeccable detector's mechanical flow warning | Replaced animated `width` with transform-based progress scaling and preserved the intentional map grid advisory; `npm run check`, `npm run build`, and `git diff --check` passed |
| `99fdce4` | Remove superseded UI vocabulary and align README routes | Deleted unused legacy flow/trust CSS and the obsolete three-entry trust model, removed stale `mode`/tab vocabulary from the tree, and documented canonical routes plus redirects in README; `npm run check`, `npm run build`, `git diff --check`, and stale-pattern search passed |
| `5e38d55` | Final implementation consistency audit | `DESIGN.md` now names the implemented shell/map/flow/trust components; `npm run check`, `npm run build`, `git diff --check`, canonical route smoke checks, context-query smoke check, and the one-time Impeccable detector pass completed. Browser screenshot inspection was attempted through the required browser surface but was unavailable, so visual screenshot evidence remains an explicit limitation. |
| `1fa3844` | Make the canonical flow deep-linkable | Added the required `/flows/[flow]` route for `packet-decode`, preserved step state in that route's URL, linked to it from the flow index, and corrected the architecture evidence note to describe the shipped eight-region fixture; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `690ed9f` | Expose hosted snapshot states | Hosted bundle requests now explicitly announce loading, unavailable/error, and valid-empty states; the illustrative fixture is hidden while a requested bundle is unresolved, and failed requests offer recovery instead of silently falling back; checks passed. |
| `704bf95` | Distinguish coverage-limited snapshots | Added typed coverage state and page/rail language for illustrative, verified graph-backed, and graph-backed coverage-limited snapshots; fixture and projection paths now expose the distinction; `npm run check` and `git diff --check` passed. |
| `e81c1ff` | Label map external boundaries | Added explicit `wire input` and `consumers` labels outside the repository map region so entry and exit points are readable without inferring them from position; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `fa29124` | Preserve map and flow history | Architecture selection now encodes level 1 and uses browser history with `popstate` restoration; flow step selection likewise uses history and restores the active step on Back/Forward; checks passed. |
| `a134391` | Make Start here map stages actionable | Replaced decorative macro-stage spans with keyboard-accessible links into each corresponding architecture region at `level=1`, while retaining the compact ordered path and responsive layout; checks passed. |
| `271d8e4` | Add semantic document footer | Added a restrained footer landmark with a contextual Lachesis continuation link and mobile reflow, completing the shell's semantic header/nav/main/aside/footer landmarks without adding competing navigation; `npm run check` and `git diff --check` passed. |
| `61f65e5` | Default flow linear mode on mobile | The accessible all-steps text fallback now opens automatically at the mobile breakpoint, tracks viewport changes, and remains user-toggleable while the guided stepper stays available; `npm run check` and `git diff --check` passed. |
| `3c44c7e` | Add shareable page metadata and links | Added a reusable Copy link control to every page intro and search-friendly title/description, Open Graph, Twitter, author, application, and keyword metadata at the root; `npm run check`, `npm run build`, and `git diff --check` passed. Clipboard behavior remains progressively optional when browser permissions deny access. |
| `2f1a50b` | Add embeddable architecture map | Added a lightweight `/embed` route for README, issue, and social embeds, reusing the bounded map and hosted snapshot states with a clear link back to the full guide; map history now respects the embed route; checks passed. |
| `1b56ee9` | Document shareable embed usage | README now lists the canonical flow and embed routes, explains when to use a normal link versus an iframe, and includes a ready-to-paste iframe snippet; route smoke testing confirmed `/embed` and social metadata (`og:title`, `twitter:card`, `Copy link`). |
| `1fd5446` | Harden share fallback | Copy link now uses the native share sheet when available, falls back to the clipboard, handles user cancellation quietly, and clearly explains when browser permissions make copying unavailable; `npm run check` and `git diff --check` passed. |
| `f9efbf2` | Add focused route metadata | Dynamic architecture-region and flow routes now emit repository/region/flow-specific titles and descriptions for search results and social previews; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `57e2058` | Audit stale vocabulary and crawler access | Stale UI terms are confined to historical/reference documentation or product vocabulary; no deprecated component/mode implementation remains. Added an explicit allow-all `robots.txt` route so public guide pages and embeds remain crawler-discoverable without inventing a deployment hostname. |
| `d761c1e` | Add social preview image | Added a generated Open Graph image with the product thesis and repository architecture vocabulary; configured `metadataBase` from `NEXT_PUBLIC_SITE_URL`/`VERCEL_URL` with a local fallback, and verified `/opengraph-image` prerenders during `npm run build`. |
| `98eb9ed` | Add index route metadata | Architecture, Flows, and Trust indexes now emit repository-specific titles and descriptions for search and social sharing rather than inheriting the generic root title; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `22f9b99` | Fix mobile map boundary labels | The mobile ordered relationship composition now hides spatial-only external boundary labels along with the diagram axes, preventing overlap and preserving a clean linear reading order; `npm run check` and `git diff --check` passed. |
| `b7ea8eb` | Avoid inferred graph relationships | Graph-backed maps no longer fabricate sequential edges when relationship evidence is absent; the map and ordered summary state that relationships are unavailable instead, preserving honest provenance; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `b687cc1` | Add technical article structured data | Root layout now emits JSON-LD `TechArticle` metadata describing the architecture field guide and its publisher, improving technical-document discovery without changing the reading UI; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `30584a8` | Preserve legacy link context | `/map` and `/flow` redirects now carry all incoming query parameters into `/architecture` and `/flows`, preserving selected regions, levels, and flow steps for shared legacy links; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `c09d328` | Add host-aware sitemap | Added a sitemap route for public guide, flow, trust, embed, and illustrative region pages using `NEXT_PUBLIC_SITE_URL`/`VERCEL_URL` with a local fallback; build verified `/sitemap.xml` generation. |
| `57d610a` | Link sitemap from robots | Configured `robots.txt` to advertise `/sitemap.xml` when a public deployment host is available while keeping local development valid without a fabricated hostname; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `339ea95` | Make unknown region links recoverable | Dynamic region chapters no longer 404 when a shared graph-backed region ID is outside the fixture; they explain the missing snapshot context and offer a Lachesis recovery link, avoiding invented architecture facts; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `0b17f51` | Preserve bundle context in region links | Architecture inspector and region-directory links now carry the active opaque bundle ID into focused chapters and Lachesis handoffs, keeping maintainer-shared graph context attached; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `3a94725` | Preserve bundle in fallback handoff | Unknown region chapters now parse their incoming bundle query and retain it in the recovery link to Lachesis, so incomplete graph-backed chapters remain actionable within the original snapshot; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `caaa8ab` | Carry identity through region links | Architecture inspector and directory links now carry repository, revision, and bundle context into region chapters; unknown-region recovery passes all three into Lachesis instead of relying on defaults; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `6452f1e` | Preserve trust filter links | Trust glossary search and category filters now persist as `q`/`kind` URL parameters, restore on open, and remain included when a filtered page is copied or shared; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `e57e5fe` | Restore trust filters from history | Trust glossary listens for `popstate` and restores URL-backed search/category state when navigating browser history, keeping shared review sessions coherent; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `4efb54e` | Make trust category history navigable | Search typing continues to replace the current URL, while deliberate category changes use `pushState`, allowing useful Back/Forward review without creating a history entry for every keystroke; `npm run check` and `git diff --check` passed. |
| `f3af308` | Make social preview repository-neutral | Global Open Graph artwork no longer hardcodes the Suricata fixture name, avoiding misleading previews when Design Map serves another repository; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `d475b78` | Support legacy copy fallback | Copy link now falls back to a temporary textarea/`execCommand` path when the native Clipboard API is unavailable, improving shareability in embedded and older browser contexts; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `11623d1` | Document public metadata host | README now explains that production deployments should set `NEXT_PUBLIC_SITE_URL` (with automatic `VERCEL_URL` support) so sitemap and social metadata never point maintainers at localhost; `npm run check` and `git diff --check` passed. |
| `6c28dcf` | Align product and design vocabulary | Updated Product to the shipped Architecture/Flows/Trust route names and documented Start/embed as distribution surfaces; updated the design system to record IBM Plex Serif as the implemented display face; no UI behavior changed. |
| `cbe862f` | Use Architecture map vocabulary | Updated the durable design guidance from retired “System Map” wording to the shipped Architecture map term, reducing naming drift for future work; `npm run check` and `git diff --check` passed. |
| `238140a` | Add copyable embed snippet | The `/embed` route now includes a compact, host-aware iframe snippet with a Copy embed code action, reducing friction when maintainers place the HLD map in a README or existing document; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `f83ca8e` | Harden embed code copy | Embed code copying now uses Clipboard API with a legacy textarea fallback and a visible unavailable state, matching the resilient Copy link behavior in restricted iframe/browser contexts; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `e424b60` | Add maintainer share copy | README now includes ready-to-adapt framing for README/issues and Twitter/LinkedIn, pointing readers to the architecture map and preserving the product’s “read before source” boundary; `npm run check` and `git diff --check` passed. |
| `1efe59c` | Add README image embed pattern | README now documents a GitHub-compatible linked Open Graph preview image in addition to the iframe, so maintainers can place a clickable HLD visual where iframes are stripped; `npm run check` and `git diff --check` passed. |
| `ba029a2` | Remove obsolete map state branches | Deleted unreachable loading/error banner branches and the unused error style after the explicit state-panel implementation, leaving one authoritative state presentation; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `e21cdab` | Remove legacy map route styles | Deleted unused decorative route/line CSS now that all map connections are data-driven SVG edges, keeping the stylesheet aligned with the current topology model; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `9f17437` | Make embed identity-aware | `/embed` now derives its title, description, heading, and footer state from incoming repository/bundle context, avoiding a misleading Suricata fixture label when a maintainer shares a graph-backed bundle; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `382f836` | Make map identity repository-aware | The accessible map canvas label now derives from the loaded snapshot repository instead of hardcoding Suricata, so hosted bundles do not expose a misleading screen-reader name; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `2bf1b23` | Keep embed surface focused | Moved the README iframe authoring snippet from `/embed` into the full Architecture guide, so an embedded map contains only its repository header, map, accessible relationship summary, and recovery links; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `4bcae08` | Preserve context across reading surfaces | Flow and Trust pages now accept shared repository/revision/bundle context, keep it in step/filter URLs, show the contextual identity while retaining the illustrative label, and include it in every Lachesis handoff; checks passed. |
| `f1331be` | Scope flow metadata to repository | Dedicated flow pages now derive their document title from the shared repository query, keeping search and social previews aligned with the visible maintainer context; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `0937bd8` | Retain context in secondary links | Flow’s handoff-contract link and Trust’s return-to-Architecture link now preserve repository, revision, and bundle parameters instead of resetting a shared review to the fixture default; checks passed. |
| `f86f5c4` | Preserve context in region chapters | Known and unknown region chapters now use contextual repository/revision labels, context-aware metadata, preserved neighbor/back links, and bundle-aware Lachesis recovery; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `4194bf4` | Explain stale graph snapshots | The repository shell now recognizes an explicit stale/outdated/superseded limitation on graph-backed bundles, labels the snapshot as stale, and gives revision-confirmation guidance without guessing freshness; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `ab7aba4` | Make Architecture context-aware | The Architecture index now derives shell identity, page metadata, thesis copy, and the next Flow link from incoming repository/revision/bundle context while keeping fixture provenance explicit; checks passed. |
| `1a6d41c` | Preserve context in shell navigation | `DocsShell` now carries shared repository/revision/bundle context through the primary rail and footer handoff, and all contextual pages pass that contract into the shell; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `6268130` | Make Start here repository-aware | The canonical `/` distribution entry now derives metadata, title, orientation copy, macro-path links, and recommended routes from incoming repository/revision/bundle context while retaining fixture provenance; checks passed. |
| `f0ff27e` | Preserve context in embed snippet | The Architecture guide’s copyable iframe now includes incoming repository, revision, and opaque bundle parameters and uses a repository-specific iframe title, so pasted embeds open the reviewed snapshot; checks passed. |
| `f935aac` | Make bundle loading honest on first render | Architecture and Embed pass the requested bundle into `MapClient`'s initial state, so server-rendered HTML shows the loading state instead of briefly exposing the illustrative map before hydration; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `1f8944c` | Sanitize embed title labels | Copied iframe markup now normalizes repository labels before placing them in the HTML `title` attribute, keeping unusual query-supplied names from producing malformed share snippets; checks passed. |
| `ceacec8` | Make semantic zoom reversible | Focused Architecture links now declare `level=1`, the map exposes a keyboard-operable `Back to system shape` action, and chapter/directory links retain the semantic level and snapshot context; checks passed. |
| `828dde5` | Guard semantic zoom URL on server | Fixed a server-render regression introduced by the zoom control by guarding `window.location` access, restoring 200 responses for queryful Architecture routes; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `f1da5d1` | Describe Lachesis handoff pages | `/explore` now emits context-aware metadata using the selected repository and anchor, improving previews for shared code-handoff links rather than inheriting the generic product title; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `e91e9a3` | Carry explicit flow steps to Lachesis | Flow handoffs now include both `flow` and `step` query fields, and Explore shows the selected step explicitly alongside repository, revision, region, and anchor; checks passed. |
| `e892ff6` | Show complete handoff context | The Lachesis confirmation page now visibly lists selected region and opaque bundle alongside repository, revision, anchor, flow, step, and trust domain when present; checks passed. |
| `8c5a16e` | Hydrate shared flow step | Flow pages now initialize the stepper from the server URL, so a shared `?step=ipv4` link renders the IPv4 narrative before hydration instead of briefly showing Ethernet; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `6a13b98` | Hydrate shared trust filters | Trust now seeds its search/category controls from the server URL and validates the requested category, so shared filtered links render the matching glossary state before hydration; checks passed. |
| `cdf3217` | Raise share control hit areas | Copy link, Copy embed code, and Trust inspect links now use a 44px minimum target, aligning the compact distribution controls with the brief’s touch/accessibility floor; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `cd3225c` | Preserve Architecture focus on first render | Architecture now passes the requested region, semantic level, and shared snapshot query into `MapClient`, so a copied focused URL renders the intended region and context-preserving reset link in SSR HTML; `npm run check`, `npm run build`, `git diff --check`, and live `curl` smoke for `?repository=Suricata&revision=8f4c1b2&region=output&level=1` passed. |
| `109c330` | Normalize invalid zoom levels | Client hydration now accepts only the documented `level=1` focus value and falls back to system shape for malformed or missing values, keeping SSR and browser state aligned on hand-edited share URLs; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `9bcd751` | Make mobile region focus navigable | Architecture selection now opens the full region chapter on small screens while preserving the selected snapshot query, and flow step content is announced as the narrative changes; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `3d389d4` | Normalize invalid Trust filters | Trust now normalizes unsupported `kind` values both on initial state and browser history restore, keeping malformed shared links aligned with the server-rendered all-domains view; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `7f0d1e5` | Make flow updates screen-reader concise | Guided flow changes now announce one atomic step status through a visually-hidden live region instead of re-reading the entire stage, preserving the visible reading layout while keeping step changes perceivable; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `1cab93c` | Add explicit design-anchor zoom | Architecture now implements the brief’s level-2 design-anchor view, limits it to twelve anchors, links each to the contextual Lachesis handoff, and provides a reversible level-2 → level-1 path; SSR smoke for `?region=decode&level=2` shows the anchor view and preserved repository/revision context, with checks passed. |
| `2026-09-05` | Sweep share and malformed route states | Live HTTP smoke returned 200 for Start, Architecture level 2, Flows deep link, Trust with an invalid kind, and Explore handoff; focused anchor and normalized Trust query assertions passed. Browser screenshots remain unavailable in this environment. |
| `8b89ddf` | Align social previews with page context | Added one shared metadata helper and applied it to Start, Architecture, region chapters, Flows, Trust, Explore, and Embed so repository-specific browser titles, Open Graph titles, and Twitter titles stay in sync; live HTML confirmed `Zeek architecture · Design Map` in both social title tags, with checks passed. |
| `98ab24b` | Preserve bounded graph child communities | The graph adapter now projects deterministic child modules into level-1 regions, caps them at twelve with explicit remainder text, carries anchors forward, and indexes nodes once for large bundles; projection smoke covered 0, 1, 8, 30, and 500 top-level modules, plus checks passed. |
| `b25001f` | Make focused children deep-linkable | Level-1 relationship rows now expose keyboard-accessible links into the explicit level-2 anchor view, preserving region, repository, revision, and bundle context instead of presenting selectable-looking but inert rows; checks passed. |
| `1ce6199` | Trial bundle-only handoff gate | Explored treating an opaque bundle as sufficient context; this was superseded by `6e3f51a` after the contract audit confirmed a selected anchor/region is required before opening Lachesis. |
| `6e3f51a` | Require selected context for Lachesis | Handoff confirmation now requires a selected region, anchor, flow step, or trust domain before opening Lachesis; bundle/repository/revision-only links retain their context on the Architecture recovery CTA without synthesizing fixture coordinates, with checks and live smoke passing. |
| `37f66d3` | Validate hosted graph bundle shape | Added a runtime `LachesisBundle` guard for format, schema, metadata, node records, and graph shape; malformed payloads now reach the designed plain-language unavailable state instead of leaking a cast/runtime exception, with valid/malformed guard smoke and checks passing. |
| `2a8a8de` | Validate graph module records | The hosted bundle guard now rejects non-array module collections and malformed module entries before projection, preventing late `.filter` failures on otherwise valid-looking JSON; malformed-module smoke, checks, and build passed. |
| `8c948af` | Clarify pending bundle provenance | Contextual pages with a requested bundle now label the shell `Graph-backed bundle requested` and explain that fixture content remains hidden/replaced only after validation, avoiding a false illustrative-versus-verified reading while loading; live Architecture smoke and checks passed. |
| `1b4a422` | Explain unknown Architecture focus | Architecture now carries the full incoming region/level query into the server render and explicitly announces when a requested region is absent from the current projection, avoiding a silent fallback to the first fixture region; checks and live unknown-region smoke passed. |
| `9618ee6` | Announce dynamic map and Trust state | Architecture level/provenance status and Trust result counts now use atomic polite status announcements, aligning dynamic URL-backed changes with the visible state for assistive-technology readers; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `0c0844f` | Add reproducible route smoke gate | Added `npm run smoke`, covering repository context, Architecture level 2, Flow step state, Trust filters, Explore handoff, Embed provenance, unknown regions, and legacy redirects; the gate passed against the local dev server alongside check/build/diff validation. |
| `1e32903` | Assert contextual social metadata | The route smoke gate now requires repository-specific Open Graph and Twitter title tags on a shared Architecture URL, preventing SEO regressions from being hidden behind a generic page-content pass; smoke, check, build, and diff validation passed. |
| `63a8e88` | Cover embed loading state in smoke | The reproducible route gate now asserts that a requested embed renders `Preparing the architecture map` before bundle validation completes, protecting the no-fallback loading contract; the gate passed after restarting a stale local dev manifest. |
| `3c90100` | Make interactive buttons explicit | Map nodes, relationship-summary controls, and Flow step selectors now declare `type="button"`, preventing accidental form submission when these reusable reading surfaces are embedded in a form context; smoke, check, build, and diff validation passed. |
| `0e03814` | Preserve context in empty map recovery | The no-region graph state now carries repository, revision, and opaque bundle parameters back to Start here instead of resetting to the fixture root, keeping sparse/empty snapshot recovery within the same review; smoke, check, build, and diff validation passed. |
| `e97f3f2` | Document verified embed context | README now includes a copyable repository/revision/opaque-bundle iframe example and explicitly labels the bare Suricata snippet as illustrative, reducing maintainer risk when publishing a generated map; `git diff --check` passed. |
| `558839a` | Preserve context on wordmark navigation | The global Design Map wordmark now retains repository, revision, and bundle query context when returning to Start here, eliminating the final shell-level context reset; `npm run check`, `npm run build`, and `git diff --check` passed. |
| `188dde5` | Connect flow selectors to their detail region | Guided-flow step controls now declare `aria-controls` and the changing narrative panel exposes a matching labelled region, making the selector-to-content relationship explicit for keyboard and assistive-technology users; `npm run check` and `git diff --check` passed. |
| `731f69e` | Announce share outcomes reliably | Share-link and README-embed controls now expose dedicated polite live statuses for successful and unavailable copy outcomes, keeping the button name stable enough for assistive technology while preserving visible feedback; `npm run check` and `git diff --check` passed. |
| `722fa5d` | Label the architecture map landmark | The reusable map workbench is now a named section with an explicit `Architecture map` heading, giving landmark navigation a reliable entry point before the visible relationship alternative; `npm run check` and `git diff --check` passed. |
| `c1289a4` | Add skip navigation to the embed surface | Standalone `/embed` now offers a keyboard skip link targeting a focusable map boundary, so README/social embeds can bypass the header and enter the architecture content directly; `npm run check` and `git diff --check` passed. |
| `8e9c6ad` | Enforce route landmark invariants | `npm run smoke` now verifies every tested document route renders exactly one `h1` and a `main` landmark, and specifically protects the embed skip target alongside its loading/provenance assertions; smoke, check, and diff validation passed. |
| `9d1af91` | Connect map selectors to the inspector | Architecture map nodes and the ordered relationship controls now declare `aria-controls="architecture-map-detail"`, matching the live inspector target so selection updates are explicit to assistive-technology users; `npm run check` and `git diff --check` passed. |
| `e0a1ebe` | Expose the full projection in the text alternative | The accessible relationship summary now lists every bounded projected region—not only the nine placed nodes—and names itself as a projection, preserving graph-derived regions and avoiding a false one-to-one claim about the canvas; `npm run check` and `git diff --check` passed. |
| `6c0657e` | Preserve focused child anchors | Level-1 child links now carry their specific anchor into level 2, where the requested anchor is promoted and marked as the current location; copied focus URLs therefore retain the reader’s exact selection instead of reopening an undifferentiated list; `npm run check` and `git diff --check` passed. |
| `6261c8b` | Mark the focused anchor item correctly | The selected level-2 anchor is now marked on its list item with `aria-current="true"`; the outbound Lachesis link no longer claims to be the current page, keeping selection semantics accurate; `npm run check` and `git diff --check` passed. |
| `08ca858` | Preserve anchor state in SSR Architecture | Architecture now passes the incoming `anchor` query into `MapClient`’s server-rendered initial state, and smoke asserts a focused level-2 URL emits `aria-current="true"` before hydration; smoke, check, and diff validation passed. |
| `70dc02a` | Restore the map reset hit area | Architecture zoom-reset links now retain the shared 44px minimum target instead of overriding it to 32px, keeping compact controls operable for touch and keyboard users; `npm run check` and `git diff --check` passed. |
| `78812a0` | Explain unknown anchor focus | Level-2 Architecture now announces when a requested anchor is absent from the current projection and shows the available anchors instead of silently implying an exact match; route smoke covers the malformed-anchor state, with check and diff validation passed. |
| `efe9f81` | Make zoom status context accurate | Architecture toolbar metadata now distinguishes placed regions, one focused region, and visible anchors at levels 0/1/2, removing the misleading “regions shown” count from focused views; `npm run check` and `git diff --check` passed. |
| `f52f586` | Make map state announcements atomic | Hosted loading/error and valid-empty panels now use atomic polite/alert announcements, ensuring assistive technology receives the complete state label, explanation, and recovery action as one coherent update; `npm run check` and `git diff --check` passed. |
| `f707046` | Announce external handoff tabs | Global and contextual Lachesis links now include accessible names that state they open in a new tab, while the visible arrow remains decorative; `npm run check` and `git diff --check` passed. |
| `c66ec4e` | Assert handoff new-tab labeling | Contextual Explore smoke now requires the explicit accessible name on the Lachesis action, protecting the external-tab behavior as distribution copy evolves; smoke, check, and diff validation passed. |
| `4734c50` | Explain unknown flow steps | Flows now announce when an incoming `step` query is absent from the canonical handoff list and show the first step as an explicit recovery state; route smoke covers the malformed-step URL, with check and diff validation passed. |
| `ebffaae` | Preserve invalid flow state in history | The malformed-step status now updates during `popstate` restoration and clears after a valid selection, so browser Back/Forward cannot silently fall back without the same recovery explanation; `npm run check` and `git diff --check` passed. |
| `fed083f` | Raise navigation hit areas | Wordmark, embed shell links, region-directory links, and focused child links now expose 44px interaction rows while retaining compact type, bringing secondary distribution/navigation surfaces under the touch target floor; `npm run check` and `git diff --check` passed. |
| `bbfc55a` | Clear stale anchors across map focus | Region changes and zoom resets now remove the previous anchor query, preventing a level-2 selection from leaking into another region’s shared URL while preserving anchors only for the focused region view; `npm run check` and `git diff --check` passed. |
| `8bc652c` | Preserve deep links beyond the placed canvas | Architecture now distinguishes the nine-region spatial canvas cap from the full projected region directory, so valid focused URLs for later projected regions render their actual region instead of silently falling back to the first node; `npm run check` and `git diff --check` passed. |
| `6c83864` | Make projection scalability reproducible | Added `npm run projection-smoke`, exercising the real adapter with 0, 1, 8, 30, and 500 top-level modules plus a 20-child roll-up; each case stayed within the documented bounds and emitted explicit remainder regions where required. |
| `4b975ca` | Protect contextual Flow and Trust previews | Route smoke now asserts repository-specific Open Graph and Twitter titles for the shareable Flows and Trust entry points, alongside their existing deep-link and filter assertions; `npm run smoke` passed. |
| `d2d352a` | Document UX verification gates | README now gives maintainers the exact `check`, `build`, projection, and route-smoke commands and explains the states each gate protects before a map is shared; `git diff --check` passed. |
| `9d0e50b` | Preserve context in preview snippet | The README’s GitHub image/link example now carries repository, revision, and opaque bundle parameters on both the preview URL and destination, preventing maintainers from publishing a generic handoff by accident; `git diff --check` passed. |
| `419b01a` | Announce unknown region recovery | Unknown `/architecture/[region]` chapters now expose an atomic polite status panel, and route smoke covers the recovery landmark so stale region links remain perceivable and actionable without client hydration; smoke, check, and diff validation passed. |
| `fbb9249` | Enter region focus on map selection | Desktop level-0 node selection now updates both the URL and React zoom state, immediately switching to the selected region’s focus view instead of requiring a reload to reconcile the semantic zoom; `npm run check` and `git diff --check` passed. |
| `75ced8b` | Reconcile map state with client navigation | MapClient now observes Next search-parameter changes in addition to `popstate`, so child-anchor links and zoom-reset links update the mounted map’s level/selection immediately instead of leaving stale visual state after client navigation; `npm run check` and `git diff --check` passed. |
| `2d3c104` | Clear anchor state for anchorless children | Child level-2 URLs now remove any inherited anchor when the selected child has no anchor of its own, preventing direct or stale level-1 URLs from falsely highlighting an unrelated design anchor; `npm run check` and `git diff --check` passed. |

## Rebuild brief audit

The Luna brief was reviewed as a specification rather than as shipped UI.

- **Ambiguity:** Pass. Each route names its reader question, required content, interaction model,
  responsive behavior, and exclusions.
- **Research traceability:** Pass. Official sources are linked inline and every reference says what
  to borrow and what not to copy. Luna must create a dated visual research record before coding.
- **Product boundary:** Pass. Design-level canonical flows remain in Design Map; function bodies,
  symbol neighborhoods, runtime taint tracing, and findings remain in Lachesis/Trace.
- **Scalability:** Pass. The specification bounds initial regions, focused children, and
  connections; requires stable roll-ups and fixtures spanning zero to 500 communities; and rejects
  a force-directed hairball.
- **Content sufficiency:** Pass. The useful Suricata thesis, macro path, subsystem inventory, boot
  path, decode flow, dispatch distinction, key structures, and trust taxonomy are retained without
  copying either Claude mockup's layout.
- **Implementation authority:** Pass. The current frontend is explicitly disposable while safe
  bundle loading and validated graph adapters are preserved when suitable.
- **Verification:** Pass for documentation. `git diff --check` completed without whitespace errors;
  the document includes build, interaction, accessibility, screenshot, and clean-worktree gates for
  implementation.

## Current checks

```bash
npm run check
npm run build
```

Static and live route checks performed against the local dev server:

- `/`, `/architecture`, `/architecture/decode`, `/flows`, `/flows?step=ipv4`, `/flows/packet-decode`, `/trust`, `/explore`, `/embed`, `/opengraph-image`, `/robots.txt`, and `/sitemap.xml` returned 200 in the latest route smoke run.
- Legacy `/map` and `/flow` returned redirects to `/architecture` and `/flows`, preserving incoming query context.
- Contextful `/explore?repository=Suricata&revision=8f4c1b2&region=decode&anchor=DecodeEthernet%28%29` rendered `Context ready`, the repository/revision, region, anchor, and Lachesis handoff.
- Contextful `/embed?bundle=b_demo123&repository=Zeek&revision=main` rendered `Zeek architecture`, `Graph-backed bundle requested`, and a Zeek-specific document title; `/embed?bundle=b_demo123` uses the honest generic `Graph-backed repository` label.
- Contextful `/flows?repository=Zeek&revision=main&bundle=b_demo123`, `/flows/packet-decode?...&step=ipv4`, and `/trust?...&q=memory` rendered the Zeek identity and preserved repository/revision/bundle in handoff links; the dedicated flow title was `Zeek · Packet decode into flow state · Design Map`.
- Contextful `/architecture/decode?repository=Zeek&revision=main&bundle=b_demo123` rendered `Packet decode · Zeek architecture` and preserved context in chapter links; an unknown region rendered `Architecture region · Zeek`, `Region context unavailable`, and a bundle-preserving Lachesis recovery link.
- Contextful `/architecture?repository=Zeek&revision=main&bundle=b_demo123` rendered `Zeek architecture · Design Map`, a Zeek repository bar, and a context-preserving `/flows` link.
- Contextful `/explore?repository=Zeek&revision=main&bundle=b_demo123&region=decode&anchor=DecodeEthernet%28%29` rendered primary rail and footer links with the same `repository=Zeek`, `revision=main`, and `bundle=b_demo123` query context.
- Handoff audit found the global footer was dropping selected context. It now preserves repository, revision, bundle, region, label, anchor, flow, step, and trust-domain fields in its `/explore` URL; route smoke asserts the focused Explore footer contract.
- Cross-origin recovery audit now has browser-level evidence: Explorer smoke reads valid, malformed, and unknown bundle responses from a separate Design Map origin, confirming the CORS fix is usable by the actual frontend rather than only visible in server headers.
- Flow handoff audit found step links were incorrectly using the step ID as the flow ID. They now preserve the canonical `packet-decode` flow alongside the selected step, with route smoke asserting `flow=packet-decode&step=ipv4`.
- Start provenance audit found a requested bundle still rendered the illustrative fixture on `/`. Start now uses an explicit graph-backed pending state, links to the validating Architecture loader and contextual Lachesis, and route/browser smoke confirms fixture prose is absent at 375px.
- Bundle provenance audit extended the same guard to Flows index, dedicated packet flow, and Trust: requested bundles now render an explicit pending state and hide illustrative steps/domains until validation; route smoke covers all three surfaces and their fixture-free HTML.
- Mobile target-size audit found the focused skip link rendered at 42px high. It now has an explicit 44px minimum; a Playwright scan of visible links, buttons, summaries, inputs, and selects across Start, Architecture, Flows, Trust, and Explore reports no remaining sub-44px targets at 375px.
- Navigation audit found the shell’s always-visible Lachesis homepage link acted like a fifth permanent section. The top-bar handoff now appears only when a region, anchor, flow step, or trust domain is selected; route smoke confirms bare Start has no `bar-lachesis` action while focused routes retain contextual handoff links.
- SEO/share audit found generated Open Graph and Twitter image URLs dropped opaque bundle IDs. `documentMetadata` and every route metadata generator now carry `bundle` through the preview URL; route smoke asserts the contextual image query on bundle-requested Start, Architecture, Flows, and Trust pages.
- Pending-state language audit found secondary bundle routes claimed they were actively “Preparing” without owning a loader. Shared Flows/Trust and Start states now say “Awaiting a verified …” and direct readers to the Architecture validator; route smoke passes across all bundle-requested surfaces.
- Mobile navigation audit found the horizontal rail clipped Trust at 375px with no discoverability cue. It now renders all four route labels in a compact four-column row, hides secondary notes only on mobile, and has zero nav/document overflow.
- Map error-state audit found failed bundle recovery used the default Suricata identity instead of the requested repository/revision. A mocked 503 at 375px now yields `/explore?repository=Zeek&revision=main&bundle=b_demo123` with zero overflow; the error remains explicit and fixture-free.
- Start CTA audit found the recommended “Follow a packet” action was below the first viewport. It now sits beside the map introduction as the single primary action (Architecture remains secondary); at 375px the CTA appears around 644px and the first map stages around 733px, with route smoke protecting both.
- Region-chapter navigation audit found the return link discarded the selected chapter and reopened the unselected system shape. It now restores `/architecture?region=<id>&level=1` with repository/revision/bundle context; route smoke verifies the decode chapter round trip.
- Focused Architecture identity audit found map-generated handoff links still used the illustrative Suricata identity when only `repository`/`revision` context was present (without a bundle). Map links and canvas labels now derive identity from the active query; browser smoke confirms Zeek-focused links carry `repository=Zeek&revision=main`.
- Typography audit found mobile cascade overrides reduced the repository revision and snapshot status to 10px, below the documented floor. They now render at 12px (as do Start numbering markers); computed Playwright checks across all primary routes report the corrected sizes.
- Runtime integration audit found Next.js warning about global smooth scrolling without its `data-scroll-behavior` root opt-in. The document root now declares the attribute; check/build/route smoke pass with the scroll behavior contract explicit.
- Contextful `/?repository=Zeek&revision=main&bundle=b_demo123` rendered `Zeek · Design Map`, the repository-led title `Zeek, before the source.`, and preserved the same context on every Architecture/Flows/Trust entry link and macro-path stage.
- Contextful `/architecture?repository=Zeek&revision=main&bundle=b_demo123` rendered an iframe snippet with `/embed?repository=Zeek&revision=main&bundle=b_demo123` and `title="Zeek architecture map"`.
- Queryful `/explore?repository=Zeek&revision=main&bundle=b_demo123&region=decode&flow=packet-decode&step=ipv4&anchor=DecodeIPV4%28%29` rendered visible `region`, `step`, and `bundle` fields plus `DecodeIPV4() · Zeek handoff · Lachesis` metadata.
- Server-rendered `/flows?step=ipv4` included the `validated IPv4 payload` narrative and `step=ipv4` state before hydration, while retaining the full ordered step list.
- Server-rendered `/trust?q=memory&kind=input` included `value="memory"`, `value="input"`, and only the matching `Memory safety` domain in the initial HTML.
- Server-rendered `/embed?repository=Zeek&revision=main&bundle=b_demo123` showed `Preparing the architecture map` and did not include the fixture relationship summary before hydration; its return link retained the Zeek bundle context.
- Queryful `/architecture?repository=Zeek&revision=main&region=decode&level=1` was re-smoked after the SSR guard and returned 200; the browser-level zoom control remains available without a server-side `window` error.
- Rendered and browser smoke confirmed `/embed` is a bounded distribution artifact containing its repository header, interactive map, provenance footer, and full-guide link without the inspector, relationship transcript, or README authoring panel; `/architecture` retains `Use this map in a README` for maintainers preparing an embed.
- Unknown `/architecture/graph%3Amodule%3Aunknown?bundle=b_demo123` returned 200 with `Region context unavailable`, `Continue to Lachesis`, and the preserved bundle ID rather than a false 404.
- `npm run check`, `npm run build`, `npm run projection-smoke`, `npm run smoke`, and `git diff --check` passed after the latest implementation; the build generated 22 routes including the share/discovery surfaces.
- Direct hosted verification now succeeds for the current demo ID: `LACHESIS_EXPLORER_URL=https://lachesis.unboundcompute.com npm run explorer-smoke` returned 200, validated the 2.0 bundle, preserved 7 relationships, and passed malformed/unknown-ID and CORS checks. The hosted artifact is still intentionally small (1 module), so this proves transport and relationship projection but not multi-region production-scale semantics.
- Earlier browser-surface inspection was unavailable in this environment; a local Playwright runner is now installed and provides the viewport evidence recorded below.
- Playwright headless viewport audit is now available locally: `/`, `/architecture`, `/flows`, `/trust`, and `/embed` each had zero horizontal overflow, exactly one `h1`, and one `main` at 375×812, 768×1024, 1024×768, and 1440×1000; a 375px Architecture full-page screenshot was captured and inspected.
- Start-page composition audit found the map was below the first mobile viewport when the structural thesis preceded it. The repository-led title/lede now introduce the map immediately, with the longer linear path following the map; mobile rhythm places the warm map canvas at roughly 772px in a 375×812 viewport so its first stages are visible, and route smoke protects the updated title/deep link.
- Playwright interaction smoke at 375px also confirmed mobile Architecture selection navigates to the selected region chapter (`/architecture/input?region=input&level=1`), Flow step selection updates both `step=ipv4` and the “validated IPv4 payload” narrative, and Trust filtering updates `q=memory` while narrowing the visible domains.
- Playwright’s 200% reflow equivalent at a 720px CSS viewport returned zero horizontal overflow on Start, Architecture, Flows, Trust, and Explore; the earlier style-zoom probe was discarded because it bypassed media-query reflow and altered the test document root.
- Playwright keyboard/reduced-motion smoke confirmed Flow step buttons receive a 3px visible focus outline under keyboard Tab navigation, Architecture map nodes do the same, and the flow progress transition resolves to `0.00001s` under `prefers-reduced-motion: reduce`.
- Clipboard smoke confirmed the focused Architecture share action copies the complete URL, including `repository`, `revision`, `region`, `level`, `anchor`, and opaque `bundle` context; the visible status changed to `Link ready`.
- Desktop visual pass at 1440px captured and inspected Start here, Flows, and Trust full-page screenshots; the editorial reading hierarchy, bounded map/flow surfaces, and restrained CTA treatment remain coherent with zero horizontal overflow.
- Latest 375px Flow screenshot inspection confirmed the branch explanation stacks before the current handoff, the duplicate step navigator is absent, Previous/Next remains operable, the linear five-step transcript is open by default, and the route has zero horizontal overflow. Desktop retains the single spatial flow-path selector.
- Latest 375px Trust screenshot inspection confirmed the long domain search and kind filter remain operable, the selected domain stays readable as a single document section, query/filter state remains visible in the URL, and zero horizontal overflow persists.
- Playwright deep-link smoke at 375px confirmed `/trust?domain=memory-safety` selects and scrolls the `#memory-safety` article to the 20px reading offset, with zero horizontal overflow.
- Playwright desktop history smoke confirmed Architecture selection semantics: selecting `Application protocols` entered `?region=protocols&level=1`, Back restored level 0 with all eight nodes, and Forward restored the focused region plus inspector content.
- Playwright mobile embed smoke confirmed selecting the first map node stays on `/embed?region=input&level=1` with zero horizontal overflow; the full Architecture route still uses the region chapter destination.
- Token contrast audit found no reader-facing paper-surface failure: `--muted`/paper is 4.56:1, `--body`/paper 6.24:1, `--signal`/paper 5.74:1, and map ink/map surface 14.54:1; the 4.23:1 axis annotation is decorative secondary map geometry and has an adjacent text relationship summary.
- Visual anti-pattern audit found no broad `transition: all`, glow/backdrop effects, or dashboard-style gradients; the remaining gradient is scoped to the map grid, and compact 10–11px hits are identifiers/indices or overridden reader metadata covered by the 12px informational floor.
- Latest mobile Architecture check at 375×812 confirmed the map reflows into a readable region list, the redundant adjacent inspector stays hidden because selection navigates to a region chapter, and the optional text projection starts closed with zero document overflow.
- Production-preview verification was repeated after rebuilding `.next`: the earlier 18–21px target readings came from a stale Next server, not the current stylesheet. The rebuilt 375px Architecture surface reports 44px quiet links/share controls, 58px relationship disclosure, 68px map nodes, and a 48px compact primary-nav target; route smoke, typecheck, and diff checks remain clean.
- End-to-end newcomer/maintainer journey was replayed against the rebuilt production preview at 375px: Start → `Follow a packet` → `validated IPv4 payload` → Lachesis preserved repository, revision, region, flow, step, and anchor; Trust search updated `q=memory`; mobile Architecture selection opened the matching region chapter with snapshot context intact.
- Narrow-width regression sweep after the compact path change covered Start, Architecture, Flows, Trust, and Explore at 320, 360, 375, and 414px: every route kept one `h1`/`main`, zero document overflow, and all visible Start interactive targets remained at least 44px.

## Acceptance checklist (current)

This matrix is intentionally conservative: “Pass” means the requirement is evidenced in the current
prototype; “Partial” marks a dependency on a live Lachesis bundle or a contract that cannot be
verified from the fixture alone.

| Requirement | Status | Evidence / remaining proof |
| --- | --- | --- |
| First viewport identifies the repository and an obvious first action | Pass | Start here renders repository-led copy and one `Follow a packet` primary action with Architecture as the secondary route; Playwright places the primary action inside the 375×812 first viewport. |
| Start, Architecture, Flows, and Trust use different reading structures | Pass | Separate route components and distinct map, flow-step, and glossary interaction models. |
| Architecture remains bounded at scale | Pass | Nine placed regions, twelve rendered connections, twelve-child/region bounds, explicit nine-region roll-ups, and projection smoke for 0/1/8/30/500 modules plus pipeline, fan-out, flat-mesh, alias, and deterministic-ranking fixtures. |
| Selection updates content, URL/history, and a Lachesis handoff | Pass | Playwright Back/Forward and focused handoff URL checks cover the rendered selection path; populated `graph.edges` project into hosted region relationships, and the separate live-endpoint row tracks the external transport dependency. |
| Flow synchronizes step, narrative, URL/history, and linear text | Pass | Mobile and desktop step smoke, malformed-step recovery, and reduced-motion checks. |
| Trust educates without becoming a vulnerability scorecard | Pass | Eight-domain glossary, obligation/non-claim copy, filters, and deep-linked selected-domain behavior. |
| Fixture facts cannot masquerade as verified evidence | Pass | Explicit provenance labels, graph-requested hiding, chapter fallback rejection, and bundle schema guards. |
| Complete Lachesis context survives handoff | Pass | Repository, revision, bundle, region, label, anchor, flow, step, and domain are encoded and smoke-tested where present. |
| Loading, error, empty, sparse, stale, and coverage states are designed | Partial | Fixture, malformed/failed, empty, sparse, stale, limited-copy, transport-read, network, expiry, and size-limit paths are implemented and locally exercised; a real hosted bundle exercising every rendered state remains external proof. |
| Mobile reflow, keyboard operation, contrast, and reduced motion pass | Pass | Playwright 375/768/1024/1440, 720px reflow, focus, reduced-motion, clipboard, and contrast evidence above. |
| Share/embed/SEO surfaces are reusable by maintainers | Pass | Contextual social previews, focused embeds, sitemap/robots, README snippets, and route smoke coverage. |
| Live Lachesis graph relationship projection is verified end-to-end | Partial | The current hosted opaque demo transport returns a valid 2.0 bundle and the adapter projects its 7 node edges; the artifact has one module, so multi-region production-scale relationship semantics remain unproven. |

## Known prototype limits

- Suricata counts and node facts are fixture data, not yet read from a Lachesis graph endpoint.
- The Lachesis query string is the agreed handoff shape; the explorer must implement its parser and focused-bundle loading.
- The default map still renders eight illustrative regions; hosted snapshots use the deterministic bounded projection and roll-up adapter, projecting optional Lachesis `graph.edges` when present and remaining explicit when relationship evidence is absent.
- `/embed` is shareable and bundle-aware, but defaults to the explicitly labeled illustrative fixture until a valid `?bundle=b_…` identifier is supplied.
